---
date: 2026-08-24
---
Folded 23 notes into `docs/parity.md` and `docs/interface-contract.md` and deleted them, and corrected the three section-3 rows that read `Today: None` for subsystems that were demonstrably on master - `workflow/*, schedule/*, jobs/*`, `lsp/*` and `code-runtime/*, e2b/*`. Those three rows are why this fold is not housekeeping: a dispatch read them, concluded the workflow subsystem had never landed, and ordered a rescue of code that was already merged. A parity document that is wrong in that direction does not merely mislead a reader, it manufactures duplicate work. Section 3 is one row per area again, with only `scripts/*` still reading None, which is correct because it is upstream's own build tooling. Section 4 gains nine port rows the notes had written - the code-runtime seam, its two backends, the four settings halves and the scoped-store half - and three amendments where a suite grew rather than appeared. The contract document gains what four notes were holding: §4.3.5 on what `delivered` means on a terminal signal (the signal reached the group; a program may still ignore it, and a surface must not render that as "stopped"), §4.7.2 recording that `crates/rpc` gained `FrameSink` and `FrameHandler` from another lane under the file-ownership rule, §4.7.3 requiring every surface to answer `catalog.tools` from the shipped assembly after one tree answered 26, 1 and 0 depending on who was asked, and a row recording that `method::AGENT_STEER` is missing from `methods::ALL` - the single mistake that constant's own doc comment says it cannot catch, left as a `crates/protocol` change rather than fixed inside a docs fold.
