---
date: 2026-08-25
---
TC-CLI-UI-18 recovered (`crates/cli/tests/presentation.rs`), the one case the flattening dropped while keeping the change it guards. PR 221's implementation is on master - `status_line` and its four call sites, the ARCHITECTURE.md paragraph, and even the test file's own module header claiming that a redirected stderr is told a turn is running whichever view stdout was asked for - and only the test was lost. Nothing else from that branch is replayed, because everything else is already here and replaying it would be a conflict fought for no gain. The case is worth having back rather than merely tidy: `--json` is the view that used to write nothing to a redirected stderr, so a script asking for machine-readable output on stdout silently lost its log, and nothing on master asserted otherwise - the only two assertions on the status line are a plain-pipe case and the model-name-cannot-drive-the-terminal case, neither of which runs `--json`. Proved twice, because a recovered test that cannot fail is a comment: it passes against master's implementation unchanged, and it fails against the pre-fix rule keyed on whether stdout is a terminal. The neighbouring gap was enumerated before closing 221 - master's ids run 1..17 then jump to 30..31, which looked like twelve casualties and is one: 19..29 appear on no branch, no ladder-preserve tag and no unreachable commit, and skipped ids are normal here, with TC-PORT-SCHEMA missing 11 and TC-PORT-REQID missing 4 for the same reason.
