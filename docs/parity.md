# Parity gaps against upstream

## 1. Identification

- **System:** tetanus (binary `tetanus`, umbrella crate `tetanus-hardness`), version 0.1.0.
- **Tracks:** deepseek-harness `0.1.0-rc.7`, commit `99f6f02fec`, consulted as a read-only reference.
- **Purpose:** name what upstream does that tetanus does not do yet, and the phase that closes each gap.
- **Status:** current as of 2026-08-19. Section 6 records every change.
- **Authoritative copy:** this file, in the tetanus repository.

This is a gap list, not a design description and not a test plan.
Design lives in [../ARCHITECTURE.md](../ARCHITECTURE.md) and [turn-flow.md](turn-flow.md).
The engine/presentation boundary lives in [interface-contract.md](interface-contract.md).
The phase plan and the parity decision live in [PLAN.md](PLAN.md).

## 2. How the numbers were taken

Upstream spec files, counted in the reference clone:

```sh
find packages apps -name '*.spec.ts' -not -path '*/node_modules/*' | wc -l   # 650
find scripts -name '*.spec.ts' | wc -l                                       # 47
```

650 spec files across 49 package families, plus 47 specs that test upstream's own build and release tooling.
tetanus's own cases each carry a `TC-*` identifier and all of them run offline.
Their count moves with every merge, so it is not kept here as a number: it is what `grep -rhoE 'TC-[A-Z0-9-]+' crates/*/tests | sort -u | wc -l` prints.
Case counts are not a parity metric on their own: one upstream spec file can hold thirty assertions, and tetanus asserts a whole event sequence in one case.
They are used here only to size an area.

Parity is functional, not protocol-level ([../ARCHITECTURE.md](../ARCHITECTURE.md) section 6).
Upstream's web contract is generated from TypeScript decorators, so its client specs do not port one for one.

## 3. Areas

`Today` is what tetanus serves on `master`.
`Closes in` is the phase from [PLAN.md](PLAN.md): ② is Cordis parity, ③ is the WASM host and the rest.

| Upstream area | Specs | Today | Gap | Closes in |
| --- | ---: | --- | --- | --- |
| `core/*` (agent-loop, session, tools, agent, system-prompt, scope) | 58 | Turn engine, session log, registry, four dispatch modes, prompt assembly as a waterfall, prompt variables registered, carried by the assembly and substituted before the model reads the text, a configured tool order with a rest entry for what it does not name, named by the settings document, resume from a cold journal, interrupt at the step boundary and in a retry wait, a failing tool and a failing classifier both contained, a completion the provider's output cap cut off ended at the cap and dispatching none of the calls it carried, a call whose arguments are not JSON dispatched with the text the model wrote, a turn closed on the journal whichever way it ended, a session forked at a closed boundary into a child that continues the conversation it inherited | Scoped stores, tool permissions, a queued inbox with steering and latched wakes, cancellation inside a step, a cut-off step steered into a later one - the arrangement that makes the cap's reason sticky within a turn - the rest of the header metadata (cwd, subagent origin, delegation depth; parent session and the seed boundary are on the journal now), fiber disposal, containment of a throwing decision listener (an observer is contained; `serial` and `waterfall` stay loud by design), runtime-context providers, scoped prompt layers, property tests for the tool pipeline | ② (port list in section 4) |
| `core/*` (agent-loop, session, tools, agent, system-prompt, scope) | 58 | Turn engine, session log, registry, four dispatch modes, prompt assembly as a waterfall, prompt variables registered, carried by the assembly and substituted before the model reads the text, a configured tool order with a rest entry for what it does not name, named by the settings document, resume from a cold journal, interrupt at the step boundary and in a retry wait, a failing tool and a failing classifier both contained, a completion the provider's output cap cut off ended at the cap and dispatching none of the calls it carried, a call whose arguments are not JSON dispatched with the text the model wrote, a turn closed on the journal whichever way it ended, the tool pipeline's scheduling stated as properties over every schedule rather than over five examples | Fork, scoped stores, tool permissions, a queued inbox with steering and latched wakes, cancellation inside a step, a cut-off step steered into a later one - the arrangement that makes the cap's reason sticky within a turn - header metadata (cwd, parent session, subagent origin, delegation depth), fiber disposal, containment of a throwing decision listener (an observer is contained; `serial` and `waterfall` stay loud by design), runtime-context providers, scoped prompt layers | ② (port list in section 4) |
| `core/*` (agent-loop, session, tools, agent, system-prompt, scope) | 58 | Turn engine, session log, registry, four dispatch modes, prompt assembly as a waterfall, prompt variables registered, carried by the assembly and substituted before the model reads the text, a configured tool order with a rest entry for what it does not name, named by the settings document, resume from a cold journal, interrupt at the step boundary and in a retry wait, a failing tool and a failing classifier both contained, a completion the provider's output cap cut off ended at the cap and dispatching none of the calls it carried, a call whose arguments are not JSON dispatched with the text the model wrote, a turn closed on the journal whichever way it ended, a decision seam a tool call can be gated on | Fork, scoped stores, tool permissions wired into the tool pipeline, a queued inbox with steering and latched wakes, cancellation inside a step, a cut-off step steered into a later one - the arrangement that makes the cap's reason sticky within a turn - header metadata (cwd, parent session, subagent origin, delegation depth), fiber disposal, containment of a throwing decision listener (an observer is contained; `serial` and `waterfall` stay loud by design), runtime-context providers, scoped prompt layers, property tests for the tool pipeline | ② (port list in section 4) |
| `core/*` (agent-loop, session, tools, agent, system-prompt, scope) | 58 | Turn engine, session log, registry, four dispatch modes, prompt assembly as a waterfall, prompt variables registered, carried by the assembly and substituted before the model reads the text, a configured tool order with a rest entry for what it does not name, named by the settings document, resume from a cold journal, interrupt at the step boundary and in a retry wait, a failing tool and a failing classifier both contained, a completion the provider's output cap cut off ended at the cap and dispatching none of the calls it carried, a call whose arguments are not JSON dispatched with the text the model wrote, a turn closed on the journal whichever way it ended, including a turn a plugin panic ended | Fork, scoped stores, tool permissions, a queued inbox with steering and latched wakes, cancellation inside a step, a cut-off step steered into a later one - the arrangement that makes the cap's reason sticky within a turn - header metadata (cwd, parent session, subagent origin, delegation depth), fiber disposal, runtime-context providers, scoped prompt layers, property tests for the tool pipeline | ② (port list in section 4) |
| `core/*` (agent-loop, session, tools, agent, system-prompt, scope) | 58 | Turn engine, session log, registry, four dispatch modes, prompt assembly as a waterfall, prompt variables registered, carried by the assembly and substituted before the model reads the text, a configured tool order with a rest entry for what it does not name, named by the settings document, resume from a cold journal, interrupt at the step boundary and in a retry wait, a failing tool and a failing classifier both contained, a completion the provider's output cap cut off ended at the cap and dispatching none of the calls it carried, a call whose arguments are not JSON dispatched with the text the model wrote, a published helper that tells a tool author how a call's arguments fail the schema it declared, a turn closed on the journal whichever way it ended | Fork, scoped stores, tool permissions, a queued inbox with steering and latched wakes, cancellation inside a step, a cut-off step steered into a later one - the arrangement that makes the cap's reason sticky within a turn - header metadata (cwd, parent session, subagent origin, delegation depth), fiber disposal, containment of a throwing decision listener (an observer is contained; `serial` and `waterfall` stay loud by design), runtime-context providers, scoped prompt layers, property tests for the tool pipeline | ② (port list in section 4) |
| `session/*`, `session-query/*` | 38 | JSONL log, session store, self-describing journal, crash repair on reopen, a record a crash cut short dropped from the file rather than the file refused with it, a listing that names the root it could not read | SQLite persistence, projections and caches, titles, telemetry, stats, checkpoints, log export and query | ② for persistence, ③ for query |
| `core/*` (agent-loop, session, tools, agent, system-prompt, scope) | 58 | Turn engine, session log, registry, four dispatch modes, prompt assembly as a waterfall, prompt variables registered, carried by the assembly and substituted before the model reads the text, a configured tool order with a rest entry for what it does not name, named by the settings document, resume from a cold journal, interrupt at the step boundary and in a retry wait, a failing tool and a failing classifier both contained, a completion the provider's output cap cut off ended at the cap and dispatching none of the calls it carried, a call whose arguments are not JSON dispatched with the text the model wrote, a turn closed on the journal whichever way it ended | Fork, scoped stores, tool permissions, a queued inbox with steering and latched wakes, cancellation inside a step, a cut-off step steered into a later one - the arrangement that makes the cap's reason sticky within a turn - header metadata (cwd, parent session, subagent origin, delegation depth), fiber disposal, containment of a throwing decision listener (an observer is contained; `serial` and `waterfall` stay loud by design), runtime-context providers, scoped prompt layers, property tests for the tool pipeline | ② (port list in section 4) |
| `session/*`, `session-query/*` | 38 | JSONL log, session store, self-describing journal, crash repair on reopen, a record a crash cut short dropped from the file rather than the file refused with it, a listing that names the root it could not read, the projection seam - a named fold per domain driven forward as events commit, with a checkpoint that is a shortcut and never an authority | SQLite persistence, the projections themselves (usage, pressure, breakdown, stats, title), telemetry, log export and query | ② for persistence, ③ for query |
| `llm/*` | 34 | DeepSeek adapter, streaming seam, token-free mock, a bounded retry policy with jitter resolved out of the settings document, a block a provider may write for its own route installed on the route that provider named, the executor that runs it against a live route, heuristic token pricing and the priced surface, a stream that ends without `[DONE]` refused, a completed response with no content refused as a retryable failure, the wait a throttled provider asks for read off its answer, an idle window on the connection so a provider that stops speaking fails instead of hanging the turn, a stalled body turned into a timeout and retried to recovery over the real transport, a product identity on every request | Further providers, a measurement anchored on real provider usage, a refused answer's provider request id and its terminal-quota-versus-throttle distinction, the three token projections, a whole-request deadline: the idle window catches silence, not a provider that answers slowly for ever | ② |
| `llm/*` | 34 | DeepSeek adapter, streaming seam, token-free mock, a bounded retry policy with jitter resolved out of the settings document, a block a provider may write for its own route installed on the route that provider named, the executor that runs it against a live route, heuristic token pricing and the priced surface, a stream that ends without `[DONE]` refused, a completed response with no content refused as a retryable failure, the wait a throttled provider asks for read off its answer, an idle window on the connection so a provider that stops speaking fails instead of hanging the turn, a whole-request deadline beside it so a provider that never stops speaking is bounded too, a stalled body turned into a timeout and retried to recovery over the real transport, a product identity on every request | Further providers, a measurement anchored on real provider usage, a refused answer's provider request id and its terminal-quota-versus-throttle distinction, the three token projections, | ② |
| `llm/*` | 34 | DeepSeek adapter, streaming seam, token-free mock, a bounded retry policy with jitter resolved out of the settings document, a block a provider may write for its own route installed on the route that provider named, the executor that runs it against a live route, heuristic token pricing and the priced surface, a stream that ends without `[DONE]` refused, a completed response with no content refused as a retryable failure, the wait a throttled provider asks for read off its answer, an idle window on the connection so a provider that stops speaking fails instead of hanging the turn, a stalled body turned into a timeout and retried to recovery over the real transport, a refusal the account cannot spend its way out of told apart from one a backoff fixes, a product identity on every request | Further providers, a measurement anchored on real provider usage, a refused answer's provider request id, the three token projections, a whole-request deadline: the idle window catches silence, not a provider that answers slowly for ever | ② |
| `client/*` | 125 | Terminal UI, owned by the presentation lane | Not a port. See section 5 | out of scope |
| `host/*` (apiproxy, webserver, static frontend, directory picker) | 33 | None; the WebSocket carrier this workspace serves instead authenticates, which upstream's webserver does not - see section 5 | HTTP host and its provider proxy | ③ |
| `subagent/*` (drivers: in-process, fork, ACP, Codex, Claude Code) | 28 | None | Subagent spawn, control and report tools | ② |
| `sandbox/*` (policy, local, Windows ACL) | 21 | The path fence `fs-sandbox` applies, ported as `tetanus_turn::fs` | The mode vocabulary (`read-only`, `workspace-write`, `danger-full-access`), the per-call escalation stamp, and the kernel backends that isolate untrusted code rather than untrusted paths | ③ |
| `fs/*` (fs, local, sandboxed, tools, observation policy) | 20 | Path containment: a workspace root, canonicalize-then-contain resolution that follows a symlink before judging it, a path that does not exist yet judged by where it would land, and an alias-equivalent root recognised by filesystem identity | The filesystem service itself - stat, read, list, the guarded write and the read-match-write edit - its tools, the observation policy, and the sandbox mode layer over the fence | ② with the tool pipeline |
| `hooks/*` (protocol, Claude Code, Codex) | 18 | In-process event bus with mode enforcement | Out-of-process hook protocol and its adapters | ② |
| `shell/*`, `terminal/*`, `subprocess/*` | 32 | Running one external command: argv without a shell, an environment the caller lists rather than one inherited and scrubbed, output bounded to its tail, and a timeout that kills and still reports what was printed | Terminating a process group rather than one child - the SIGTERM-to-SIGKILL ladder, so grandchildren die too - streaming stdio for a protocol consumer, spill files, persistent shells, PTY and terminal tools, and the model-facing shell tool itself | ② |
| `extensions/*` (Cordis host and client runners) | 14 | Compile-time registry instead | Out-of-tree plugins need a host | ③ (WASM component host) |
| `workflow/*`, `schedule/*`, `jobs/*` | 24 | None | Workflow workers, scheduling, job store | ③ |
| `lsp/*` | 12 | None | LSP stdio client and its tool | ③ |
| `compaction/*` | 12 | The tool-result pruner's content transform: a deterministic head/marker/tail shrink measured in characters, with a budget that is refused unless it can converge | Applying it to a turn's history, the session transaction that records a prune durably, and model-driven compaction of the conversation itself | ② |
| `web/*` (fetch, search providers) | 11 | None | Web fetch and search tools | ② |
| `typert/*` | 11 | Hand-written contract in `crates/protocol` | Not a port. See section 5 | out of scope |
| `preset/*` (agent presets, persona) | 9 | None | Named agent presets | ② |
| `interaction/*` (approvals, questions, commands, permission presets) | 9 | The approval seam: a session policy folded from its own journal over a deployment default, a one-to-one durable audit pair per question, an answerer chain that fails closed on every way of not getting an answer, an interrupt that withdraws an outstanding question, and a question a crash caught mid-flight closed on reopen | Wiring the seam into the tool pipeline so a tool can require it, `approval.set` served over the boundary, permission presets, user questions, commands | ② |
| `settings/*`, `boot/*` | 15 | Layered config with provenance, the settings document under the harness home, its re-read at run time, boot resolution, the engine's own settings resolved out of the document, including the parallel tool cap every turn runs under, a credential in that document withheld from `config.dump` | A file watcher to drive the re-read, per-namespace schemas - which is also what would let a scalar written where a section belongs be refused rather than ignored (TC-PORT-SET-5) - redaction driven by a schema rather than by a name, writing the document back | ② |
| `interaction/*` (approvals, questions, commands, permission presets) | 9 | None | Permission gates on the tool pipeline, user questions | ② |
| `settings/*`, `boot/*` | 15 | Layered config with provenance, the environment layer that fills it (`TETANUS_SECTION__KEY`), the settings document under the harness home, its re-read at run time, boot resolution, the engine's own settings resolved out of the document, including the parallel tool cap every turn runs under, a credential in that document withheld from `config.dump` | A file watcher to drive the re-read, per-namespace schemas - which is also what would let a scalar written where a section belongs be refused rather than ignored (TC-PORT-SET-5) - redaction driven by a schema rather than by a name, writing the document back | ② |
| `acp/*`, `sdk/*`, `api/*` | 17 | Own JSON-RPC contract in `crates/protocol`, carriers in progress | ACP bridge, SDK client, gateway | ③ |
| `storage/*`, `spill/*`, `credentials/*` | 15 | Environment credentials for one provider | Key-value stores, spill policy, credential store | ② |
| `context/*`, `guard/*` | 8 | None | Time, tmux and instruction context; timeout and repeat guards | ② |
| `skill/*`, `todo/*`, `goal/*`, `plan/*`, `feedback/*`, `attachment/*`, `workspace/*` | 32 | None | The built-in feature tools | ② |
| `mcp/*` | 4 | None | MCP client | ② |
| `code-runtime/*`, `e2b/*` | 11 | None | Worker-thread code runtime, remote sandbox backend | ③ |
| `test-support/*`, `bundle/*`, `examples/*`, `util/*`, `identity/*`, `runtime-diagnostics/*` | 32 | Conformance harness with its own mock adapter | Mostly upstream plumbing. The LLM mock server and replay already have tetanus equivalents | out of scope |
| `apps/cli` | 7 | `tetanus run`, plus the presentation lane | Subcommand parity, per [interface-contract.md](interface-contract.md) section 4.7 | ② |
| `scripts/*` | 47 | None | Upstream's own build, lint and release gates | out of scope |

## 4. Next ports

These upstream suites test behaviour that tetanus surfaces today, so they port now rather than after a phase lands.
A port restates the upstream case against the tetanus seam that carries the same decision; it is not a transcription.
Closing a row updates it here and in section 3, in the same PR.

| Upstream spec | Ports to | Asserts | State |
| --- | --- | --- | --- |
| `core/agent-loop/tests/loop.spec.ts` | `crates/turn/tests/upstream_loop.rs`, `crates/turn/tests/upstream_max_tokens.rs`, `crates/engine/tests/max_tokens.rs` | The tool result reaches the next request; `agent/pre-step` fires once per proposed step with its coordinates; an empty assembly omits the system message; a replayed journal derives the same history; what a completion the output cap cut off ends, dispatches and leaves behind | ported: TC-PORT-LOOP-1, -2, -4, -8, and TC-PORT-CAP-1..5 for the cap, beside TC-CAP-1 and -2 for the same turn as a caller sees it. It found one defect: a cut-off step already dispatched nothing, but the calls it carried stayed on its `assistant/message`, so the derived history asked a provider for a `tool/result` that would never come and the next request on that session would have been refused. Upstream's sticky case - a cut-off step steered into a later step that finishes - has nothing to restate: phase ① has no inbox to steer with, so a cut-off step always ends its turn |
| `core/agent-loop/tests/interception.spec.ts` | `crates/turn/tests/upstream_loop.rs` | A rewritten claim is what gets recorded; a refused call never runs and the model is told why | ported: TC-PORT-LOOP-3, -6 |
| `core/agent-loop/tests/request-error.spec.ts` | `crates/turn/tests/upstream_loop.rs` | A provider failure ends that turn and no more | ported: TC-PORT-LOOP-7 |
| `core/agent-loop/tests/tool-order.spec.ts` | `crates/turn/tests/upstream_loop.rs` | Canonical tool order whatever the registration order, and what a configured order does instead | ported: TC-PORT-LOOP-5, -9, -10. Upstream asserts on the logged `request/header` as well as the dispatched request; tetanus logs no header, so the request is the whole assertion |
| `core/system-prompt/tests/tool-order.spec.ts` | `crates/turn/tests/upstream_loop.rs` | The rest entry, the placement it settles, and every order it refuses | ported: TC-PORT-LOOP-9..13. Upstream can only find an unregistered name while a turn assembles, because its plugins register tools later, and that turn closes with no step; a tetanus registry is settled first, so `ToolOrder::new` refuses the order and no engine is built. Upstream's stable sort between two tools sharing a name is unrepresentable in a registry keyed by name. `tools.order` in the settings document names one (`crates/engine/src/tools.rs`, TC-ORDER-1..5) |
| `core/agent-loop/tests/tool-calls.spec.ts` | `crates/turn/tests/upstream_tool_calls.rs` | Grouping, barriers, the parallel cap, and results committed in model order | ported: TC-PORT-TOOL-1..5. Upstream's two reclassification cases are not restated: they change a tool's mode while a step is running, and tetanus fixes the registry at boot as an immutable `Arc` |
| `core/agent-loop/tests/settings.spec.ts` | `crates/engine/tests/parallel_cap.rs` | The parallel tool cap a document sets, over the compiled default | part ported: TC-PARALLEL-1..5. Until now `TurnConfig::max_parallel_tool_calls` was a value only a composer in Rust could set, so the pool TC-PORT-TOOL-4 pins was unreachable from the one place a deployment states what it wants. Upstream refuses a non-positive cap at the write, because its settings service writes; tetanus reads a document, so it refuses the same value when the settings are resolved and no engine is built. The composed agents array upstream keeps out of its document has no counterpart: an adapter or a tool is registered in Rust, not named in the document, and a provider that detaches is its watcher (phase ②) |
| `core/agent-loop/tests/cancel.spec.ts` | `crates/engine/tests/interrupt.rs` | Interrupt during a step, and the state the session is left in | part ported: TC-PORT-CANCEL-1..5, with TC-AGENT-8 and TC-AGENT-9 pinning two more. An interrupt lands at the step boundary, so it cannot skip a tool already dispatched, and it does not relabel a turn whose last step already answered |
| `core/agent-loop/tests/resume.spec.ts` | `crates/engine/tests/resume.rs` | Resuming a session from its log | part ported: TC-PORT-RESUME-1..3. Upstream's file is mostly its agent factory - identity registration, abort signals, transactional setup and rollback - which tetanus does not have, so those cases have nothing to restate |
| `core/agent-loop/tests/request-reconstruction.spec.ts` | `crates/turn/tests/upstream_request_reconstruction.rs` | That the sequence of dispatched requests is one growing conversation, and that the log alone reproduces it | part ported: TC-PORT-REQ-1..3 for append-extension within a turn, append-extension across turns, and upstream's theorem restated over messages. Upstream rebuilds the whole request by folding `request/header`; tetanus logs no header, so the envelope - model, tools, system text, sampling - has nothing to rebuild from, and upstream's capacity records, reasoning-effort defaults and provider-switch cases have nothing to restate. A frozen request has no counterpart: each step is handed an owned value |
| `core/agent-loop/tests/contract-regressions.spec.ts` | `crates/turn/tests/upstream_regressions.rs` | The named regressions upstream keeps pinned | part ported: TC-PORT-REG-1..4, with TC-AGENT-6 pinning one more. The rest of upstream's file is about surfaces tetanus has not built - fiber disposal, a steering inbox, a `finish {kind:error}` chunk - so those cases have nothing to restate |
| `core/agent-loop/tests/contract-regressions.spec.ts` (`plugin exceptions are contained`) | `crates/core/tests/containment.rs`, `crates/turn/tests/upstream_regressions.rs` | An observer with a bug cannot take the engine down with it, and a decision listener with a bug cannot wedge a session | ported: TC-PORT-CONTAIN-1..5 at bus level, TC-PORT-REG-4 for an observer at turn level, TC-PORT-REG-5 and -6 for a decision listener. The bus still keeps `serial` and `waterfall` loud, and that is not the thing upstream's case is really about: what it contains is the blast radius. Until now a panicking decision listener escaped `run_turn`, so `turn/start` sat unbalanced and the session was wedged open - a reader could not tell the turn was over, and the next open synthesized `interrupted` closers for a turn nothing interrupted. It is now `TurnError::Plugin`, carrying the panic's own words, with the journal closed the way section 4.4.2 says every failed turn closes, and the engine usable for the next turn (TC-PORT-REG-6). `tetanus_engine::convert::turn_error` maps it to `Internal` (TC-FAULT-10), which the exhaustive match made a compile error to forget |
| `core/agent-loop/tests/coverage-edges.spec.ts` | `crates/turn/tests/turn_flow.rs`, `crates/turn/tests/upstream_tool_arguments.rs` | The loop edges only a listener can reach, and what a turn does with tool-call arguments that are not an object | part ported: TC-TURN-9 and TC-TURN-10 for a claim rejected after work has run and a claim that enters nothing. Upstream needs a plugin to inject a message before a second step is proposed at all; a tetanus step that asked for a tool is already owed one. TC-PORT-ARGS-1..4 for its two `tool JSON parse` cases, at the decoder and at the loop; that behaviour was already right and had no case, so these pin it. What is left is either JavaScript's own - a non-`Error` throw, a value thrown from a plugin rendered as a coded error - or a surface tetanus has not built: a driver reservation, fiber disposal, presentation metadata on a tool result, a steering waiter, and a `turn/end` append the log refused |
| `core/session/tests/session.spec.ts`, `surface.spec.ts` | `crates/session/tests/upstream_session.rs`, `crates/turn/tests/upstream_history.rs` | The append-only log contract, replay, citations, and what derives to a message | ported: TC-PORT-SESS-1..7, TC-PORT-HIST-1..4 |
| `core/session/tests/session.spec.ts` (`SessionStore`) | `crates/engine/tests/store.rs` | Session creation, listing, and the event surface | part ported: TC-PORT-STORE-1..5, alongside TC-SESS, TC-PAGE, TC-PATH and TC-ID in `sessions.rs`. tetanus reopens a known id instead of refusing it, and has one `session.create` rather than a `prepare`/`enter`/`announce` lifecycle, so upstream's rollback, reentrancy and disposal cases have nothing to restate |
| `core/session/tests/fork.spec.ts` | `crates/engine/tests/fork.rs` | What a child inherits from a prefix of its parent, and every boundary that is refused | ported: TC-PORT-FORK-1..12, beside TC-FORK-1 and -2. Upstream's child is a `Session` object seeded with frozen copies of its parent's events; a tetanus child is a journal, so a copy on disk is detached by construction and what the freeze existed for is restated as the consequence - appending to either journal afterwards leaves the other exactly as it was. Its `session/end-seed` marker has no counterpart: `fork_seq` on the header states the same boundary, so the child's own work begins at `fork_seq + 1`. Three refusals are unrepresentable: `through_seq` is a `u64`, so upstream's negative, fractional and unsafe-integer boundaries are refused by the codec before the engine is reached, and its detached and stale `Session` objects have nothing to restate, because the wire names a session by id. Upstream's corrupted-source case answers differently rather than not at all: a tetanus source is either the in-memory log its writer built or a journal `replay` refuses, so a seq gap is `LogCorrupt` naming the line, before any boundary is considered |
| `core/session/tests/scoped.spec.ts` | `crates/turn/tests/upstream_scoped.rs` | Who hears a session's events, and what awaiting `flush` promises | ported: TC-PORT-SCOPE-1..6. Upstream answers the first question with Cordis scopes - a listener on a scoped context hears only the sessions minted in that scope - and tetanus answers it structurally, with one `EventBus` per session, so the scope keys, the minting plugin and the parent chain have nothing to restate and the property they exist to give does. The flush half ports as it stands: `TurnEngine::flush` dispatches `session/flush` to every participant and waits for all of them. One claim is answered differently rather than ported: upstream propagates a rejecting flush listener, because its caller owns the failure policy, and `parallel` is a contained mode here by design, so a participant with a bug cannot fail a barrier it merely attended - the half both agree on, that a failure must not starve the listeners after it, is what TC-PORT-SCOPE-3 pins. Upstream's `prepare`/`enter`/`announce` lifecycle, its detached-carrier cases and its `session/created` and `session/disposed` notifications have no counterpart |
| `session/session-persistence-jsonl/tests/jsonl.spec.ts` | `crates/session/tests/upstream_session.rs` | Which damage to a stored journal is the crash tail, and which is committed data | part ported: TC-PORT-SESS-8..10, beside TC-PORT-SESS-3. It found one defect: a journal whose last record a crash cut short could not be opened at all. tetanus fsyncs one record per append, so the newline is the commit and the only record a crash can cut is the last one; upstream batches its appends, so it scans for the last committed `turn/end` to bound the tail instead, and its seq-gap cases follow from that boundary rather than from the file. Packed chunk rows, snapshot artifacts, revision races and the v0 log formats have no counterpart. Encoding a session id into a path segment is `crates/engine`'s: an id outside `[A-Za-z0-9._-]` is refused rather than encoded |
| `core/session/tests/properties.spec.ts` | `crates/turn/tests/properties.rs` | What holds for every journal, not for one fixture | ported: TC-PROP-SESS-1..5 for zero-based contiguous `seq`, replay reproducing both the log and its derivation, trace events never reaching the model under any interleaving, and a derived message the wire can carry. Upstream's frozen-projection property has nothing to restate: the derivation hands back an owned `Vec`, so the role, the tool-call identity and the untouched log are what is left to pin. TC-PROP-SESS-5 states the drop of a silent assistant message as a count over any log rather than as one example |
| `core/agent-loop/tests/properties.spec.ts` | `crates/engine/tests/properties.rs` | What holds for every run of prompts, not for one fixture | part ported: TC-PROP-AGENT-1..3 for one turn per accepted prompt, turn numbers counting up from one, one user message per turn, and section 4.6's states alternating over a whole run. Upstream queues a prompt that arrives while a turn is running, so its burst property is that every message gets a turn; tetanus refuses that prompt (`SessionBusy`), so the same property restates as the rule that survives the difference - a prompt is either a turn of its own or an error, never a message the journal drops. The queue itself stays phase ② |
| `core/session/tests/invariant.spec.ts` | `crates/turn/tests/upstream_session_invariants.rs` | That every journal the engine writes is well formed, whatever the turn did | part ported: TC-PORT-SESSINV-1..5 for turn numbering without a gap, one turn and one step open at a time, steps restarting inside each turn, every record inside the open step, a record that names a turn and a step naming the open ones, and a tool result answering a call of its own step - checked over a plain turn, three turns on one session, a contained tool panic, a turn a provider failure ended, and an interrupt at the step boundary. Upstream states the rules in a validator each append passes through, so its refusal cases have nothing to restate: tetanus has no validator, and the claim is about the writer. Two rules are unrepresentable rather than unported: `tool/call` and `tool/result` carry no turn or step, so their place is their position between the step's own boundaries, and a result names the call it answers rather than the step |
| `core/session/tests/repair.spec.ts` | `crates/turn/tests/upstream_repair.rs`, `crates/engine/tests/sessions.rs` | An interrupted turn is closed, not left dangling | ported: TC-PORT-REPAIR-1..10 for the synthesis and its commit, TC-SESS-6 for repair on reopen, TC-PORT-SESS-3 for the torn tail |
| `core/scope/tests/scope.spec.ts` | `crates/core/tests/effects.rs` | Composite teardown order, and a scope disposed more than once | part ported: TC-EFFECT-2, -3. Scope keys, scoped dispatch and the scope parent chain have no surface, so the rest of the file has nothing to restate |
| `settings/settings-file/tests/local.spec.ts` | `crates/config/tests/upstream_settings.rs` | Where the settings document lives, what parses, and what fails loud | part ported: TC-PORT-CFG-1..10 for boot and reads. Upstream's persist half - owner-only permissions, atomic replace, comment-preserving writes - has no surface, because tetanus reads the document and never writes it |
| `settings/settings/tests/redact.spec.ts` | `crates/config/tests/secret.rs`, `crates/engine/tests/catalog.rs` | Which values a published configuration withholds, and what it still says about them | part ported: TC-SECRET-1..4 for the rule and TC-CFG-SECRET-1..4 for the dump that applies it. Upstream reads a schema role (`secret`, `credential-ref`); tetanus has no schema for a key it does not settle, so the rule reads the last word of the key and `docs/interface-contract.md` §4.3 publishes it. Upstream's `secrets` list of every secret slot with `set` saying whether it was filled has nothing to restate: a tetanus dump lists the keys a document holds, so an unset key has no entry to describe, and the layers it redacts one by one (`base`, `user`) are one resolved list here |
| `settings/settings-file/tests/watcher.spec.ts` | `crates/config/tests/upstream_recompose.rs` | What a re-read does to a running configuration, and what a bad one must not do | part ported: TC-PORT-CFG-11..16 for the fold, the fallback of a dropped key, the last good document surviving a bad edit, a deleted document, an unchanged re-read, and a higher layer standing. The watcher itself - its debounce, its dispose quiesce, its write path, and recovery from a watcher error - has no surface, so those cases have nothing to restate |
| `settings/settings/tests/settings.spec.ts` | `crates/config/tests/upstream_settings_merge.rs` | What a document overrides and what it leaves alone | part ported: TC-PORT-SET-1..5 for the read side - one leaf written keeps the rest of its section, a list is replaced whole, an empty section sets nothing, and a key written with no value is a null the user set. Upstream reaches the same answers through a per-namespace schema and a `deepMerge`; tetanus resolves flat dotted keys, so the merge is the flattening plus the layer order and there is no merge step to test. TC-PORT-SET-5 pins where the two part: a scalar written where a section belongs is refused upstream and ignored here, because refusing a key no reader claims needs the schema this row's remaining half is about. The write side - `update`, `mutate`, `publish`, `watch`, revisions and conflict detection - is the `writing the document back` gap of section 3 |
| `core/tools/tests/tools.spec.ts` | `crates/turn/tests/upstream_tools.rs` | An unknown tool and a tool that panics both come back as failures the model reads, and neither ends the turn | part ported: TC-PORT-TOOLS-1..5. The rest of the file is the pipeline tetanus has not built - post-execute projections, presentation metadata, permission decisions and composite dispatch - so those cases have nothing to restate. Upstream's error `code` and `name` fields have no counterpart: the `ToolError` variant is the class |
| `core/tools/tests/properties.spec.ts` | `crates/turn/tests/properties_tools.rs` | What holds for every schedule of one step's tool calls, not for five of them | part ported: TC-PROP-TOOL-1..6 for one answer per call in model order, an exclusive call alone at every instant it is in flight, a pool that holds its cap without dropping work, a result that cites the call it answers however the two orders interleaved, a panic contained to its own call inside a running pool, and a journal that is a function of the schedule rather than of the race. Upstream's file is about a schema DSL tetanus does not have - a tool declares its JSON Schema directly, and nothing validates arguments against it - so its `required` fold, its totality claims and its spec-to-validator composition have nothing to restate; these are the pipeline properties section 3 named as the gap instead. A mutation check confirms the suite bites, and each mutation is caught by the property that states the rule it broke: removing the barrier break from `run_tool_group` fails TC-PROP-TOOL-2 and shrinks to a two-call counterexample - one parallel-safe call still in flight when the exclusive one starts, under a cap of two - which is the case no example covers, because TC-PORT-TOOL-2 has a barrier under the default cap and TC-PORT-TOOL-4 has a cap with no barrier; ignoring the cap fails TC-PROP-TOOL-3; committing in completion order fails TC-PROP-TOOL-1 and -5 |
| `interaction/user-approval/tests/approval.spec.ts`, `invariant.spec.ts` | `crates/turn/tests/upstream_approval.rs`, `crates/turn/tests/upstream_repair.rs` | Whether one tool call may run, who is asked, what the policy does before anyone is asked, and what the journal records | part ported: TC-PORT-APPR-1..12, with TC-PORT-REPAIR-11..13 for the closer. The enclosure precondition ports with a different reason rather than the same one: upstream batches appends and scans back to the last `turn/end`, so a bare event between turns is crash-tail garbage, while tetanus fsyncs one record per append and a between-turn event is durable. The tetanus reason is repair - the turn is the unit `session.create` balances, so a question outside one could never be closed - and TC-PORT-REPAIR-11 is the closer that makes the requirement pay. Upstream's scoped dispatch, carrier key and foreign-scope cases have no counterpart: it routes questions with Cordis scopes so a UI answers only for the agents it owns, and tetanus has one bus per session, so that containment is structural. Its borrowed-request identity case is unrepresentable - a tetanus request is an owned value - so the fields are asserted instead; its observer-throws-after-append pair is `crates/core`'s containment (TC-PORT-CONTAIN-1..5) and its disposal case is `EffectHandle`'s. Its rogue-return normalization moves to where the rogue value can actually arrive: an answerer returns a Rust enum, so the case is `ApprovalOutcome::parse` at the wire boundary the contract fixes. A mutation check confirms the suite bites, and every fail-closed rule is caught by the case that states it: a terminal that grants fails TC-PORT-APPR-2 and -4; the `never` gate moved behind the dispatch fails TC-PORT-APPR-8 and -9; removing the panic containment fails TC-PORT-APPR-5 and -12; dropping the enclosure check fails TC-PORT-APPR-1; an unknown word read as a grant fails TC-PORT-APPR-6 and -12; and dropping the repair closer fails TC-PORT-REPAIR-11 and -12. Not ported for want of a surface: the live policy-switch notice upstream injects as a user message, and the runtime-context sentence it contributes to the system prompt |
| `fs/fs-sandbox/tests/containment.spec.ts`, the path half of `fs-sandbox.spec.ts` | `crates/turn/tests/upstream_fs_containment.rs` | Whether a path the model chose is inside the workspace it was given | part ported: TC-PORT-FSC-1..12 for the root and its descendants, an absolute path out, a `..` traversal out and one that returns, a symlink out, a new file judged by the directory it would be created in, a sibling sharing the root's name prefix, an alias-equivalent root, a root that cannot be resolved, a path under a regular file, and a chain of links judged at its end. Upstream's mode half - `read-only`, `workspace-write`, `danger-full-access` and the per-call escalation stamp - is a policy layer over this one and has no surface yet, so it stays in section 3. Its Windows 8.3 and case-insensitivity cases have no alias form on this platform, so the identity fallback they motivate is exercised through a symlinked root instead (TC-PORT-FSC-8). Two cases are additions rather than ports, because the shape cannot arise upstream: TC-PORT-FSC-6 pins that a link met *after* a missing segment is still followed, which node's text-normalizing `resolve` makes unreachable there and which is exactly what a one-shot "normalize then canonicalize the parent" implementation gets wrong; TC-PORT-FSC-7 pins the separator-terminated prefix, because the string form of that comparison is a classic escape. TC-PORT-FSC-9 restates upstream's TOCTOU case with the same honesty - it pins a direction, not a guarantee. Writing the cases found one defect: a workspace rooted at a regular file was accepted, and would have contained exactly one path while refusing every other. A mutation check confirms the suite bites: never following a symlink fails five cases, comparing the prefix as a string rather than by component fails TC-PORT-FSC-7, and removing the fence fails nine |
| `core/tools/tests/tools.spec.ts` (`validateArgs`), `json-schema.spec.ts` | `crates/turn/tests/upstream_schema.rs` | What a tool author is told when a call's arguments do not fit the schema the tool published | part ported: TC-PORT-SCHEMA-1..10 for totality over any value, required keys, extras and `additionalProperties: false`, primitive types with every mistake reported rather than the first, `enum` and `const`, nesting with the path carried into the message, `integer` accepting a whole number however written, `oneOf` matching exactly one branch, and an unrecognised declaration constraining nothing. It is a helper and not a gate, which is upstream's own shape rather than a shortcut: `validateArgs` is exported for tool authors and upstream's loop only parses, which is why its `tool JSON parse` case asserts that a call whose arguments are not JSON still reaches its tool. Wiring it into dispatch was tried and backed out - it fails TC-PORT-ARGS-3, the port of exactly that case - so TC-PORT-SCHEMA-12 states the deliberate gap and points at where the decision lives; a hard gate is a behaviour change needing a contract clause first. Upstream's author-boundary throws have nothing to restate: it owns its schemas through a DSL, while a tetanus schema is a value a tool hands over, so refusing what this validator has not learned would make a working tool report violations of a rule nobody is checking. Upstream's DSL and its conversion are not ported at all; tetanus tools declare JSON Schema directly, which is the document that DSL produces. A mutation check confirms the suite bites: never checking `required` fails three cases, `oneOf` accepting more than one branch fails TC-PORT-SCHEMA-8, and dropping the nested path fails four |
| `session/session-projection/tests/registry.spec.ts` | `crates/session/tests/upstream_projection.rs` | A named fold over the journal, driven forward as events commit, and the checkpoint that makes it a cache rather than a recomputation | part ported: TC-PORT-PROJ-1..12 for the drive, a unit registered after events flowed catching up, the empty log at seq -1, only a changed value being reported, the watermark that stops a second drive folding anything twice, a detached checkpoint carrying each unit's version and watermark, restore folding only the tail past a usable row, and the two rows restore refuses - a version that is not this unit's, and one claiming events the log does not have. Upstream's registry is per-context and keys its cells by session; a tetanus session owns its log, so `Projections` is per-session and the "drives independently per session" case is structural rather than asserted. Its merge-extensible `SessionProjectionMap` type table has no Rust counterpart - a key is a string - and the typed-registration cases go with it, as do the zod `schema` validation of a view and the Cordis reference-counted sharing and fiber-unload disposal. One case is a difference rather than an omission: upstream skips downstream work when `apply` returns the same *reference*, so a unit rebuilding an equal state still notifies there, while comparing the view by value reports strictly fewer changes and never one that did not happen (TC-PORT-PROJ-4). A mutation check confirms the suite bites: adopting a stale version fails TC-PORT-PROJ-8, accepting a row that reaches past the log fails TC-PORT-PROJ-9, ignoring the watermark fails six, and reporting every unit as changed fails six |
| `compaction/compaction-tool-result-pruner/tests/tool-result-pruner.spec.ts` | `crates/turn/tests/upstream_prune.rs` | Shrinking a tool result that is too long to keep whole, without asking a model | part ported: TC-PORT-PRUNE-1..9 for the inclusive threshold, the head and tail kept around a marker, characters as the unit of both measurement and slicing, a zero head and tail still shrinking to the marker alone, a budget refused where it is set unless it can converge, one-pass convergence over a spread of budgets and lengths, the defaults, and purity. Upstream's tool results are lists of typed content blocks, so the half of its suite about preserving non-text blocks across a removed span has nothing to restate: a tetanus `ToolOutcome` carries a `String`. Its session-transaction half - rewriting the journal with a shadow node citing the result it replaced, and pricing it - needs a durable event type this contract has not published, so it stays phase ②. Its unknown-config-key rejection is unrepresentable: a `PruneBudget` is a struct, so a key nobody declared does not compile. TC-PORT-PRUNE-9 is an addition, pinning the purity that upstream's "replays to the identical pruned model messages" depends on. A mutation check confirms the suite bites, and the first one is the bug the port exists to prevent: slicing by bytes instead of characters *panics* on a UTF-8 boundary and fails TC-PORT-PRUNE-3 and -7, measuring in bytes fails TC-PORT-PRUNE-4 and -7, and accepting an unshrinkable budget fails TC-PORT-PRUNE-6 |
| `subprocess/subprocess-local/tests/spawn.spec.ts` | `crates/turn/tests/upstream_process.rs` | Running one external command, bounded in output and in time | part ported: TC-PORT-PROC-1..10 for the two streams captured apart, a non-zero exit as a result rather than an error, a command that cannot start as an error naming the program, the working directory, output bounded to its tail, standard input written and always closed, a budget that kills and says so, arguments passed as argv, and a truncated capture that is still valid text. Upstream's termination ladder is the gap: it kills a process group with SIGTERM then SIGKILL, so its trapped-signal, grandchildren, zombie-group and idempotent-terminate cases have no counterpart while this kills only the child it started - a process-group call needs a platform dependency this workspace does not have, and section 3 carries it rather than a case passing for the wrong reason. Its `pipe` and `inherit` stdio modes, offset-based non-consuming readers and spill files serve a streaming consumer this has not built. Its credential scrub is inverted rather than ported: the child gets what the caller listed, so there is no inherited environment to scrub, and TC-PORT-PROC-6 pins that with the escape hatch beside it. Two cases are additions: TC-PORT-PROC-9 pins that arguments never pass through a shell, which is what decides whether a tool can be built on this at all, and TC-PORT-PROC-10 pins that a tail cut at a byte bound still yields valid text. A mutation check confirms the suite bites: inheriting the environment fails TC-PORT-PROC-6, keeping the head instead of the tail fails TC-PORT-PROC-5, and leaving standard input open fails TC-PORT-PROC-7 - by hanging until the budget, which is the symptom the case exists to prevent |
| none - upstream has no equivalent | `crates/turn/tests/deepseek_deadline.rs` | The second bound on a request: how long it may run, however talkative it is | TC-DS-DEADLINE-1..4, closing the `llm/*` gap section 3 named rather than porting a suite. Upstream ships only an idle window, and an idle window resets on every byte: a route answering something every few moments for ever is a turn that never ends, which is the same unbounded turn the idle window was added to prevent, reached the other way round. The scripted cases run a real loopback endpoint that trickles keep-alive comments for ever, under an idle window deliberately set *longer* than the deadline, so the window cannot be what ends the request and anything that does is the deadline. Both bounds report `TIMEOUT`, because a retry policy should ask again either way; only the message differs, and it differs because "quiet" and "slow" are the one fact the failure is good for. A mutation check confirms the suite bites: removing the deadline hangs both scripted cases, and reporting both bounds with one message fails both. The hang is why those cases carry a test-level bound of their own - a lost liveness property produces no value rather than a wrong one, so without it a regression would wedge a CI run instead of failing it |
| none - upstream reads no environment | `crates/config/tests/env_layer.rs` | Settings a deployment supplies without writing a document | TC-ENV-1..8. There is no upstream suite: upstream reads a document and a service, so this is written against a hole in tetanus's own documented stack. `Layer::Env` has existed since the layered config did and the CLI has always rendered `env` as a provenance, but nothing ever put a key there, so the four-layer stack was three. `__` separates segments and a single `_` stays inside a key, because every settled key is sectioned and several are multi-word - one separator cannot tell `agent.max_parallel_tool_calls` from `agent.max.parallel.tool.calls`. A name with no `__` sets nothing, which is what keeps `TETANUS_HOME` the harness home rather than a setting called `home`. A value is JSON when it parses and text otherwise, so a numeric setting supplied by environment resolves as a number instead of failing type resolution; TC-ENV-4 pins the quoting escape that costs. TC-ENV-7 pins that a credential supplied this way is caught by the same redaction rule as one written in a document, since supplying a provider key by environment is the ordinary way to run a container. A mutation check confirms the suite bites: treating a bare name as a key fails TC-ENV-2, splitting on a single underscore fails seven, and taking every value as text fails three |
| `llm/llm/src/error.ts` (`isQuotaExceededError`, `isContextWindowExceededError`) | `crates/turn/tests/upstream_quota.rs` | Telling a terminal provider refusal from a transient one | ported: TC-PORT-QUOTA-1..6. An exhausted account arrives as a 429, the same status a provider uses for "slow down", so until now a dead key spent the whole retry budget - every attempt and every backoff - and then reported `RATE_LIMIT`, sending the reader after a throughput problem instead of a billing one. `LlmError::code` now reads what the provider said before the status it said it under, because the status is the coarser fact. Upstream matches with regular expressions; this normalizes and then matches phrases, because the workspace has no regex dependency, and TC-PORT-QUOTA-2 pins the normalization that makes the two equivalent. The negative half keeps it safe and is asserted as hard as the positive: a classifier leaning towards "terminal" would fail turns a backoff would have fixed, so `limit` is deliberately not read as a resource. TC-PORT-QUOTA-5 asserts terminality as behaviour rather than as a name, through the default retryable set. A mutation check confirms the suite bites: not classifying quota fails two cases, reading a rate limit as an exhausted account fails two, and dropping normalization fails five. The provider request id remains a gap |
| `core/tools/tests/execution-mode.spec.ts` | `crates/turn/tests/upstream_execution_mode.rs` | Whether one pending call may run beside its siblings | ported: TC-PORT-MODE-1..8. Upstream parses arguments against the schema before it asks the classifier, so an argument the schema refuses is exclusive without the tool being consulted; tetanus hands the classifier the arguments as they arrived, so the same answer comes from the classifier, and TC-PORT-MODE-5 pins it. Two cases are unrepresentable rather than unported: `ToolMode` is a two-variant enum, so a classifier cannot return a truthy non-boolean and the mode union has no type-level shape to assert |
| `core/system-prompt/tests/system-prompt.spec.ts` | `crates/turn/tests/upstream_system_prompt.rs` | Assembly inputs and their order | part ported: TC-PORT-PROMPT-1..29 for section order, empty sections, waterfall composition, short-circuiting, removal on drop, one assembly per step, the named registry - explicit order, per-assembly providers, duplicate names, disposal - and a section registered as the whole prompt, restored after the waterfall. Upstream fails an assembly that finds two complete sections effective in different scopes; tetanus has no scopes, so it refuses the second registration instead. Prompt variables are registered on the same registry, substituted by `interpolate` (TC-PORT-PROMPT-16..26), and carried by the assembly the engine renders (TC-PORT-PROMPT-27..29). Upstream throws out of the render its caller drives; tetanus renders inside the step, so a reference the assembly cannot fill fails the turn and the journal closes it. Upstream iterates its variables live, so a variable one provider registers joins the same assembly; tetanus snapshots the set first, as it does sections, so it joins the next one. Upstream's two prototype-pollution cases restate as one: a `BTreeMap` has no prototype, so a name like `constructor` is an ordinary name. Runtime-context providers and scoped layers have no surface; a non-finite order is unrepresentable in an `i32` |
| `llm/llm-retry/tests/invariant.spec.ts` | `crates/turn/tests/upstream_retry_invariants.rs` | What a journal must say about a retry chain | part ported: TC-PORT-RETRYINV-1..5 for numbering that restarts in the next step and the next turn, one started record per scheduled attempt and never before it, every record inside the turn and step it names, and a bound that counts what a resumed journal already carries. Upstream enforces these when a record is appended; tetanus has no append-time validator, so each claim is asserted against the journal a real turn writes. Upstream's payload refusals - an empty `code`, a `status` outside the range - have nothing to restate: the executor builds the record out of typed values, so there is no untyped append to refuse. Its `retryId` has no counterpart either: a chain is identified by the turn, step and provider every record already carries |
| `llm/llm/tests/retry-policy.spec.ts`, `llm/llm-retry/tests/retry.spec.ts` | `crates/turn/tests/upstream_retry_policy.rs`, `crates/turn/tests/upstream_retry_executor.rs` | Which failures are worth another attempt, the wait before it, and who acts on the decision | part ported: TC-PORT-RETRY-1..10 for the decision - the defaults, the bounded and unbounded modes, capped exponential backoff with symmetric jitter, and a provider-asked wait - then TC-PORT-RETRYX-1..7 for the executor that serves it, beside TC-RECOVER-1..3 for the recovery point itself. Upstream's validation cases are restated against the settings document as TC-RETRY-1..5 in `crates/engine/tests/retry.rs`, and TC-RETRY-6 and -7 pin that the document's policy is what a live session route actually runs; upstream's per-provider block is read as `llm.providers.<name>.retry` (TC-RETRY-8..13) and installed on the route its provider named (TC-RETRY-14..16), which is upstream's whole arrangement: there every route reads its own provider's configuration |
| `llm/llm-retry/tests/transport-recovery.spec.ts` | `crates/turn/tests/upstream_transport_recovery.rs` | What a bounded retry does when the provider fails the way a socket fails | part ported: TC-PORT-XPORT-1..6 drive the real adapter over a scripted loopback endpoint - a refused connection that recovers when the endpoint starts during the wait, a body cut off mid-message retried without committing what it streamed, a content-less completion retried, a clean end with no `[DONE]` refused and not retried, the retry budget exhausted, and a stalled body the idle window turns into `TIMEOUT` and retries to recovery. What is left is upstream's whole-request deadline: tetanus's idle window catches a provider that goes silent, not one that answers slowly for ever. That gap is in the `llm/*` row of section 3 |
| `llm/token-meter/tests/token-meter.spec.ts` | `crates/turn/tests/upstream_tokens.rs` | What content costs under the fixed heuristic, and what the surface carries | part ported: TC-PORT-TOKEN-1..10 for the density, rounding, framing per block and per role, tool calls, nested tool results, the tool catalog, a whole request, and the append-only fold. A measurement that anchors on real provider usage needs the request envelope on the log, and tetanus logs no `request/header`; replacing a range of surface nodes is compaction. Both stay phase ② |
| `llm/token-meter/tests/token-usage-projection.spec.ts`, `context-breakdown-projection.spec.ts` | none yet | Usage, pressure and breakdown projections over a session | unblocked: the seam they need is `tetanus_session::projection` (TC-PORT-PROJ-1..12). What is left is the three units themselves - each is an `init`/`apply`/`view` over the journal - plus the request envelope the usage anchor wants, which is still missing for the reason the `llm/*` row gives |
| `llm/llm-deepseek/tests/serialize.spec.ts` | `crates/turn/tests/upstream_deepseek_wire.rs` | What a message and a tool catalog look like on the official wire | part ported: TC-PORT-DS-1..4, beside TC-DS-WIRE-1 and -2. It found one defect: a tool result with no output went out as blank content instead of the sentinel upstream sends. Upstream's block-structured content - mixed text and results in one message, plugin block types, rejected images - has no counterpart in a tetanus message, and the thinking-mode fields have no surface |
| `llm/llm-deepseek/tests/translate.spec.ts` | `crates/turn/tests/upstream_deepseek_wire.rs` | What the decoder does with a frame that is short of something | part ported: TC-PORT-DS-5..9, beside TC-DS-SSE-1 and TC-DS-DECODE-1 and -2. It found one defect: a stream that never stated a finish reason reported an empty one. The empty-response classification landed with the stream that carries it (TC-DS-EMPTY-1..3, row below); the cache-hit usage fields have no surface here |
| `llm/llm-deepseek/tests/sse.spec.ts` | `crates/turn/tests/deepseek_adapter.rs` | How a stream ends, and what the sentinel closes | ported: TC-DS-CLOSE-1..4 for a stream that ends without `[DONE]`, an empty one, an unterminated `[DONE]` tail and a mid-event close; TC-DS-DECODE-3 for a frame that follows the sentinel; TC-DS-EMPTY-1..3 for a stream that ended cleanly and said nothing, against one that only reasoned and one that only called a tool; TC-DS-SSE-1 already pins event splitting and the dropped comment line. It found two defects: a cut stream returned half an answer as an answer, and a frame after `[DONE]` still appended to it. Upstream reports comment lines to a callback; tetanus has no keep-alive observer, so it drops them |
| `llm/llm-deepseek/tests/adapter.spec.ts` | `crates/turn/tests/deepseek_adapter.rs`, `crates/turn/tests/deepseek_idle_timeout.rs` | The structured facts a refused answer carries, the wait it asks for, and how long a provider may say nothing | part ported: TC-DS-WAIT-1..7 for the delta-seconds and IMF-fixdate forms of `Retry-After`, the values that ask for nothing, the wait reaching the failure a policy reads, and both forms of answer over the production transport on a loopback socket. It found one defect: no adapter read the header, so `RequestFailure::provider_retry_after_ms` was always `None` and the policy's provider-asked wait could never fire on a real route. TC-DS-IDLE-1..4 for a provider that answers and then goes quiet, one that stalls part way through a stream, the keep-alive comments that hold a stream open past one window, and the window a route runs with. It closed one gap: nothing bounded a silent connection, so a provider that stopped speaking was a turn that never ended. The watchdog is `read_timeout` on the connection rather than a wait around the decoded frames, because a keep-alive comment decodes to no frame and a window measured in frames would cut a stream that is alive. Upstream refuses a window of zero where it reads its configuration; this adapter has no fallible constructor, so it reads a zero as the default. The provider request id, the context-window and terminal-quota classifications and the thinking-mode configuration have no surface, so those cases have nothing to restate |
| `llm/llm/tests/api-key.spec.ts` | `crates/turn/tests/upstream_credentials.rs` | Whether a stored credential can be carried on the wire, and what to say when it cannot | ported: TC-PORT-KEY-1..6. It found one defect: the key was judged untrimmed, so a value of nothing but whitespace read as present and went to the provider. The judgement lives on the one adapter that resolves a credential; it moves to a shared seam when a second adapter needs it |
| `llm/llm/tests/properties.spec.ts` | `crates/turn/tests/properties_stream.rs` | What holds for every provider stream, not for one recorded one | ported: TC-PROP-STREAM-1..6 for one tool call per index named, an answer that restates the chunks it streamed, a fold that is a function of the frames alone, the last finish reason stated or `stop`, a sentinel that ends the answer, and a decoder that refuses arbitrary text rather than panicking on it. Upstream assembles typed content blocks and pins `blocks()` as stable across repeated calls; `StreamDecoder::finish` consumes the decoder, so a second call is unrepresentable and what is left to pin is that nothing outside the frames reaches the answer. Upstream's block tags have no counterpart: a `StreamChunk` variant is the tag |
| `llm/llm/tests/attribution.spec.ts` | `crates/turn/tests/upstream_attribution.rs` | Which product a request says it is, and the one header that says it | ported: TC-PORT-ATTR-1..4, beside TC-PORT-ATTR-5 for the header on a real request. Upstream takes an identity per call site through an options argument; tetanus has no configuration surface for one, so `AppIdentity` is a parameter and the default is the only identity anything constructs. It found one defect: tetanus sent no `User-Agent` at all, so every request was anonymous traffic |

Suites deliberately not ported yet, because the surface does not exist: the `scoped.spec.ts` of `core/tools` and `core/system-prompt`, the rest of `core/tools`.
They are listed with their phase in section 3.

## 5. Deliberately out of scope

- **Upstream's unauthenticated web host.** `packages/host/webserver` performs no authentication and no origin check, and takes `'127.0.0.1' | '0.0.0.0'` as configuration, so any peer that reaches the port drives the harness. tetanus does not match this. A connection to `crates/rpc`'s WebSocket carrier can start turns, read every journal in the server's directory and read the resolved configuration, and the standing deployment rule is that served surfaces bind `0.0.0.0` for off-box access - so an unauthenticated port is a defect rather than a parity target. `tetanus_rpc::auth` requires a token from every non-loopback peer, checks `Origin` in every posture, and refuses before the upgrade; there is no posture that admits everyone. The one property its suite cannot pin is the constant-time token comparison: replacing it with `==` passes every case, because the difference is in how long a refusal takes rather than in what it answers, and a timing assertion would be flaky without proving anything.

- **`client/*` (125 specs).** Upstream's client is a web UI bound to a generated protocol. tetanus owns its surfaces and its UI is not a port of theirs, so these specs measure a product decision rather than a gap.
- **`typert/*` (11 specs).** Type generation for that same protocol. `crates/protocol` is written by hand and tested by `crates/protocol/tests/wire.rs`.
- **`scripts/*` (47 specs).** Upstream's build, lint and release gates. tetanus has its own, in `.github/workflows` and [../CONTRIBUTING.md](../CONTRIBUTING.md).
- **`bundle/*`, `examples/*`, most of `test-support/*`.** Packaging and demos.

Out of scope means it does not gate parity.
It does not mean the capability is refused: if one of these turns into a real requirement, it arrives as a phase item in [PLAN.md](PLAN.md) first.

## 6. Changelog

Every change to this file adds a row to [parity-changelog.md](parity-changelog.md).

It lives in its own file so that slices in flight stop colliding on it: the table
is append-only, git merges by line, and two branches appending after the same last
line conflict on every rebase. That file is marked `merge=union`, so an append is
merged rather than adjudicated. Its header states the one caveat that comes with
that driver.
