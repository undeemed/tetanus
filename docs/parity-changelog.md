# Parity changelog

Every change to [parity.md](parity.md) adds a row here, newest last.
This is the append-only half of that document, and it is a separate file for a
mechanical reason rather than an editorial one.

**Why this is not a section of `parity.md`.**
Every slice in flight appends one row to the end of this table, and git merges
by line. Two branches that both append after the same last line conflict every
time either of them is rebased, whatever they actually changed - so a queue of
five unrelated slices produced five conflicts per merge, all of them in the same
place, none of them a real disagreement.

**How that is fixed.**
[`.gitattributes`](../.gitattributes) marks this file `merge=union`, git's
built-in driver that keeps both sides of an overlapping change in order instead
of raising a conflict. That is exactly what resolving one of these by hand
amounted to, so the driver does it and nobody adjudicates an append again.

**The one thing to know about that.**
Union merge never reports a conflict, so it cannot warn about a change that is
not an append. Editing or reordering an existing row on two branches at once
would keep both versions silently. Rows here are historical facts, so they are
written once and not revised; a correction is a new row that says what it
corrects. `parity.md` itself keeps ordinary merge semantics, because its
sections 3 and 4 are edited in place and a conflict there is information.

| Date | Change |
| --- | --- |
| 2026-08-19 | First list, against upstream `0.1.0-rc.7` (`99f6f02fec`). |
| 2026-08-19 | First seven cases ported from `agent-loop` into `crates/turn/tests/upstream_loop.rs`. Two gaps they exposed added to the `core/*` row. |
| 2026-08-19 | The log contract and history derivation ported from `core/session` (TC-PORT-SESS-1..7, TC-PORT-HIST-1..4, TC-PORT-LOOP-8). Crash repair named as the remaining `repair.spec.ts` gap. |
| 2026-08-19 | Crash-repair synthesis implemented (`crates/turn/src/repair.rs`) and ported (TC-PORT-REPAIR-1..9). |
| 2026-08-19 | The closers are committed when a cold journal is reopened (TC-PORT-REPAIR-10, TC-SESS-6), closing the `repair.spec.ts` row. |
| 2026-08-19 | System-prompt assembly ported as far as the surface reaches (TC-PORT-PROMPT-1..7). It found one defect: `SystemPrompt::text()` did not drop empty sections. The section registry, variables and context providers added to the `core/*` gap. |
| 2026-08-19 | Resume ported as far as the surface reaches (TC-PORT-RESUME-1..3): a continued journal, the carried transcript, and repair that happens once. |
| 2026-08-19 | Cancellation ported as far as the surface reaches (TC-PORT-CANCEL-1..5). The queued inbox, steering and in-step cancellation it does not cover moved into the `core/*` gap column. |
| 2026-08-19 | The `SessionStore` half of `session.spec.ts` ported as far as the surface reaches (TC-PORT-STORE-1..5). Header metadata and observer isolation added to the `core/*` gap column. |
| 2026-08-19 | The named regressions ported as far as the surface reaches (TC-PORT-REG-1..3): publication order, tool-call identity, and request routing. Disposal and listener containment added to the `core/*` gap column. |
| 2026-08-19 | Effects compose: `EffectScope` unwinds newest first, nests, and finishes past a panicking undo, and `Registry::start_all` rolls a failed mount back (TC-EFFECT-1..6, TC-PLUGIN-1..2). |
| 2026-08-19 | The retry policy implemented (`crates/turn/src/llm/retry.rs`) and ported (TC-PORT-RETRY-1..9). The executor that runs it and settings resolution named as the remaining `llm/*` gap. The section 2 case count was stale at 103; it is now a command to run rather than a number to maintain. |
| 2026-08-19 | Heuristic token pricing and the priced surface implemented (`crates/turn/src/tokens.rs`) and ported (TC-PORT-TOKEN-1..10). The usage anchor, compaction's surface replacement and the three projections named as the remaining `token-meter` gaps. |
| 2026-08-19 | The DeepSeek wire contract ported as far as the surface reaches (TC-PORT-DS-1..9). It found two defects: a tool result with no output went out blank, and a stream that stated no finish reason reported an empty one. |
| 2026-08-19 | Credential judgement ported (TC-PORT-KEY-1..6) and normalized before use. It found one defect: a blank stored key read as present and spent a real request to be refused. |
| 2026-08-19 | Observer panics are contained (`emit` and `parallel`) and ported (TC-PORT-CONTAIN-1..5, TC-PORT-REG-4). `serial` and `waterfall` stay loud on purpose, so the `core/*` gap now names only the throwing decision listener. |
| 2026-08-19 | The named prompt-section registry implemented (`crates/turn/src/prompt.rs`, service key `system-prompt`) and ported (TC-PORT-PROMPT-8..12). The engine's base prompt is now a registered section like any other. Variables, runtime context and scoped layers named as the remaining `system-prompt` gaps. |
| 2026-08-19 | The settings document read into the file layer (`crates/config/src/{file,home}.rs`), ported as far as reading reaches (TC-PORT-CFG-1..10). The duplicate `llm/*` row in section 3, left by two ports landing on it, folded back into one. |
| 2026-08-19 | One step's tool calls are scheduled rather than run one by one (TC-PORT-TOOL-1..5): a bounded pool for parallel-safe calls, an exclusive call as a barrier, and results committed in model order. Permissions are the remaining half of the pipeline. |
| 2026-08-19 | The settings document can be re-read at run time (`crates/config/src/recompose.rs`) and the runtime half of `watcher.spec.ts` ported (TC-PORT-CFG-11..16). A bad edit keeps the last good configuration. The watcher that would drive it is the remaining `settings/*` gap. |
| 2026-08-19 | A tool body that panics is contained as that call's failure (`crates/turn/src/tools.rs`), and the first cases ported from `core/tools` (TC-PORT-TOOLS-1..5) pin it beside the unknown-tool message. |
| 2026-08-19 | A stream that ends without `[DONE]` is refused as `PROTOCOL` (`crates/turn/src/llm/deepseek.rs`), a frame after the sentinel decodes to nothing, and `sse.spec.ts` ported (TC-DS-CLOSE-1..4, TC-DS-DECODE-3). The empty-response classification named as an `llm/*` gap that needs a contract PR before it can land. |
| 2026-08-19 | `agent/request-error` added to the turn: a failed model request is offered to a listener before it ends the turn (TC-RECOVER-1..3). It is the seam upstream's `llm-retry` package hooks; tetanus has no listener for it yet. |
| 2026-08-19 | A prompt section may declare itself the whole prompt (`Section::complete`), restored after the assembly waterfall (TC-PORT-PROMPT-13..15). Variables, runtime context and scoped layers are what is left of the `system-prompt` row. |
| 2026-08-19 | A retry policy is resolved out of the settings document (`crates/engine/src/retry.rs`, TC-RETRY-1..5): upstream's keys under `llm.retry`, upstream's rules, and every refused value naming the key that holds it. The six keys are published in the defaults layer, so `config.dump` shows a policy nobody configured. Installing the resolved policy on a route is the next step; upstream reads it from each provider's own configuration block, and tetanus's document has no per-provider section yet. |
| 2026-08-19 | The retry executor implemented (`retry::install`, on the `agent/request-error` recovery point) and ported (TC-PORT-RETRYX-1..5). Each scheduled retry is durable before its wait, so the attempt count is read back from the journal. Settings resolution is what is left of the `retry.spec.ts` row. |
| 2026-08-19 | The resolved policy is installed on every session route (`crates/engine/src/agent.rs`, TC-RETRY-6 and -7), so a document is now the only thing that decides whether a failed request is tried again. Until this, `llm.retry` parsed, reported provenance and changed nothing. The executor is scoped to the route the session named, which is where a per-provider block will hook when the document grows one. |
| 2026-08-19 | A journal root that cannot be read is answered as one: `session.list` carries the path in its `Io` failure (contract §4.5), and only the default root reads as an empty history (TC-SESS-7..9). Before this, a root that was a file lost the path and a mistyped root reported no sessions yet. Reported by the presentation lane as issue #150. |
| 2026-08-19 | Strict `{{name}}` interpolation implemented (`prompt::interpolate`) and ported as TC-PORT-PROMPT-16..23. A reference to an unregistered name, to a registered name with no value, and text that opened a reference it never closed are all refused; a lone `{{` with no `}}` after it stays prose, and a substituted value is never scanned again. The registry that holds the variables, and the assembly that carries them to the model, are the next two slices. |
| 2026-08-19 | The engine resolves its own settings out of the document (`crates/engine/src/boot.rs`, TC-BOOT-1..5): the four keys `catalog::key` names, over the compiled defaults, with a value of the wrong type refused rather than ignored. Until now the reader in `crates/config` had no caller outside its own tests, so `config.dump` reported provenance for keys no document could set. Calling it from the binary is the presentation lane's wiring. |
| 2026-08-19 | A configured tool order ported (`ToolOrder`, `TOOL_ORDER_REST`, `TurnConfig::tool_order`; TC-PORT-LOOP-9..13), closing both upstream `tool-order` specs. The order is read against the registry it arranges, so a name nobody registered is refused before an engine exists rather than closing a no-step turn. A settings key for it is the remaining gap. |
| 2026-08-19 | `LlmError::EmptyResponse` ported (TC-DS-EMPTY-1..3, TC-PORT-RETRY-10): a DeepSeek completion that ends on a clean `stop` with no text, no reasoning and no tool call is a failure rather than a blank answer. `EMPTY_RESPONSE` was already in the default retryable codes with nothing able to raise it, so the defaults now match what the adapter can produce. |
| 2026-08-20 | A turn a failure ended is closed on the journal (`TurnEngine::close`, TC-CLOSE-1..5): the step the failure interrupted gets its `step/end`, then `turn/end` reads `stop_reason: "failed"`. Until now a failed turn returned its error and left `turn/start` open, so the state machine `docs/interface-contract.md` §4.6 documents held only for a turn that succeeded, and the next open synthesized closers reading `interrupted` for a turn nothing interrupted. Upstream closes the same two in a `finally` (`packages/core/agent-loop/src/agent.ts`). The reason is a value on the journal, not a `StopReason` variant: a failed turn produces no `TurnOutcome` to carry one. |
| 2026-08-19 | The session log's invariants ported as properties (`crates/turn/tests/properties.rs`, TC-PROP-SESS-1..5), the first `properties.spec.ts` this workspace has an answer for. `proptest` joins the workspace as a dev-dependency. A mutation check confirms the suite bites: dropping the empty-assistant-message rule from `derive_messages` fails TC-PROP-SESS-3 and -5 and shrinks to the one-event counterexample. |
| 2026-08-20 | A document names the tool order (`crates/engine/src/tools.rs`, key `tools.order`, TC-ORDER-1..5). Until now `TurnConfig::tool_order` was a value only a composer in Rust could set, so the ported order was unreachable from the one place a deployment states what it wants. An order the registry cannot serve is refused when the settings are resolved, with the rule's own message, and `config.dump` publishes the key even when nothing sets it. |
| 2026-08-20 | A retry wait ends when the turn is interrupted (`crates/turn/src/interrupt.rs`, TC-INT-1..4, TC-PORT-RETRYX-6..7). Until now a cancel that arrived during a backoff was honoured when the wait ended, so asking a turn to stop could take the whole delay; and a cancel that beat the policy still wrote an `llm/retry` promising an attempt nobody would make. |
| 2026-08-20 | The prompt-variable registry implemented (`PromptRegistry::variable`, `PromptRegistry::variables`) and ported as TC-PORT-PROMPT-24..26. A name no reference could carry is refused at the registration that made the mistake, not at the render that trips over it. The assembly that carries the variables, so section text is interpolated before the model reads it, is the next slice. |
| 2026-08-20 | A provider may write its own retry block (`retry::provider_policy`, `retry::provider_policies`, key `llm.providers.<name>.retry`, TC-RETRY-8..13). Until now one policy served every route, so a document could not say that one provider rate-limits and another times out. A block is the whole policy for its route rather than a patch on the general one, because layering the two would hand `mode: always` a `max_retries` its author never wrote - the rule TC-RETRY-5 refuses. Installing a resolved block on that route is the next slice. |
| 2026-08-20 | A tool's concurrency classifier is contained (`ToolRegistry::mode`, TC-PORT-MODE-1..8). Until now only a tool's body was contained, so a panic in the classifier - which runs first, before anything the model reads - unwound into the scheduler and took the turn down. It now answers exclusive, which is the answer that overlaps nothing, and the call still runs. |
| 2026-08-20 | `config.dump` withholds a credential the settings document holds (`tetanus_config::secret::names_a_secret`, `Catalogs::dump`, TC-SECRET-1..4, TC-CFG-SECRET-1..4). Until now every key the caller resolved was echoed with its value, and the document already has an `llm.providers.<name>.*` namespace, so a key written there was printed by `tetanus config` and answered to whatever client was on a carrier. The rule is the one `docs/interface-contract.md` §4.3 publishes: the last word of the key decides, because the engine has no schema for a key it does not settle. |
| 2026-08-20 | The retry chain's journal invariants ported (`crates/turn/tests/upstream_retry_invariants.rs`, TC-PORT-RETRYINV-1..5). They pin what `retry::install` promised and nothing asserted end to end: a step and a turn each open their own chain, a scheduled attempt is announced once and only after it was promised, no retry opens a step, and the bound is read back off the journal, so a session resumed mid-chain does not get its retries again. |
| 2026-08-20 | A turn the provider's output cap cut off ends at the cap (`StopReason::MaxTokens`, `ModelResponse::truncated`, TC-PORT-CAP-1..5, closing upstream's max-tokens cases in section 4). Until now `finish_reason` was journalled and read by nothing, so a completion that stopped mid-sentence ended `natural` - a reader was told the model had finished - and the half-written calls it carried were dispatched with arguments nobody could know were complete. It found a second defect on the way: dropping the dispatch is not enough, because the calls stayed on the `assistant/message`, so the derived history asked the provider for a `tool/result` that would never come and the *next* request on that session would have been refused. The anchor now carries the text the model did write, the provider's own finish reason and no calls (`docs/interface-contract.md` §4.4.2). |
| 2026-08-20 | Every dispatched request is pinned against the journal it went out on (TC-PORT-REQ-1..3). The suites so far asserted one request at a time; nothing said that the sequence only ever grows, or that `derive_messages` over the log prefix reproduces what the adapter was handed. Both are what makes a replayed session honest rather than merely re-readable. |
| 2026-08-20 | A throttled provider's `Retry-After` is read off its answer (`deepseek::retry_after_ms`, TC-DS-WAIT-1..7). The retry policy has honoured a provider-asked wait since it was ported, but nothing ever asked: the transport dropped the header, so every 429 fell back to local backoff and the shortest wait a rate-limited route could take was the one tetanus guessed. Both forms RFC 9110 defines are read; a value that is zero, past or unreadable asks for nothing, because refusing costs one backoff and obeying an uninterpretable value could park a route. |
| 2026-08-20 | A claim refused after work has run closes the turn that work entered (TC-TURN-9), and a claim rewritten to an empty message list closes one that entered nothing (TC-TURN-10), opening `coverage-edges.spec.ts` in section 4. The two pin opposite sides of one rule the engine already held and nothing asserted: the terminal checkpoint runs for a turn that spent a step, whatever ended it, and never for a turn that never entered one. No source change. |
| 2026-08-20 | A record a crash cut short is dropped rather than refused with the journal that holds it (`crates/session/src/lib.rs`, TC-PORT-SESS-8..10, opening `jsonl.spec.ts` in section 4). Until now any unparsable line failed the whole read, so a session interrupted mid-append could not be opened again - and the repair in `crates/engine` that closes an interrupted turn never got the chance to run. The newline is the commit: an append writes one record and fsyncs it, so a file that does not end in one ends in a fact no caller was told was durable. Reopening truncates those bytes, so the next append lands on a record boundary and leaves no seq gap. A damaged line the writer did finish is still refused, by line number. |
| 2026-08-20 | A document names the parallel tool cap (`agent.max_parallel_tool_calls`, TC-PARALLEL-1..5, opening `core/agent-loop/tests/settings.spec.ts` in section 4). It is the tool-order gap again: the cap reached `TurnConfig` only from a composer in Rust, so no deployment could ask for serial dispatch. Zero is refused when the settings are resolved rather than honoured as a pool that can start nothing, and `config.dump` publishes the key even when nothing sets it. `Catalogs::new` now takes the resolved `EngineConfig` instead of a list of values, so a key the engine settles cannot be added and then forgotten by the dump. |
| 2026-08-20 | The agent loop's scheduling ported as properties (`crates/engine/tests/properties.rs`, TC-PROP-AGENT-1..3), the second `properties.spec.ts` this workspace has an answer for. A burst is raced on a multi-threaded runtime rather than interleaved on one, so how many of it the engine accepts varies between runs and every assertion holds for whichever split happened. No engine change: it found nothing, which is the expected result for a rule three example cases already pin. |
| 2026-08-20 | The provider stream decoder's invariants ported as properties (`crates/turn/tests/properties_stream.rs`, TC-PROP-STREAM-1..6). Model based: a case generates the frames as data, renders them onto the wire and folds them a second time itself, so the assertion is against a restatement of the rule rather than against the decoder's own output. No adapter change: the recorded streams in `deepseek_adapter.rs` had already found the two defects here (a cut stream, and a frame after the sentinel), and the properties now state them for every stream. |
| 2026-08-20 | The assembly carries the prompt variables (`SystemPrompt::variables`, `SystemPrompt::render`, `AssemblePrompt::variables`; TC-PORT-PROMPT-27..29, TC-FAULT-10). Until now section text reached the model with its `{{name}}` references still in it, because the registry that resolved the values and the render that would have used them were never joined up. Substitution is the last step, so a `system-prompt/assemble` listener may add a name or replace a value and the model reads what it left. A reference the assembly cannot fill is `TurnError::Prompt`, mapped to `Internal`: the request never left, and retrying sends the same sections through the same registry. |
| 2026-08-20 | A provider's own retry block is installed on its route (`EngineConfig::provider_retry`, `Runtime::policy_for`, TC-RETRY-14..16). Until now the document could state a block per provider and every route still ran the general one, so the keys TC-RETRY-8..13 resolved changed nothing a turn did. A route whose provider wrote a block never reads the general block at all, in either direction: the block may allow a retry the general one refuses, and refuse one it allows. |
| 2026-08-20 | Bounded retry ported across a real HTTP/SSE boundary (TC-PORT-XPORT-1..5): the adapter, the decoder, the recovery point and the executor all run against a scripted loopback endpoint. It found one gap: no request or idle timeout, so a stalled provider is a turn that never ends. |
| 2026-08-20 | The session log's enclosure rules ported (`crates/turn/tests/upstream_session_invariants.rs`, TC-PORT-SESSINV-1..5). Upstream enforces them in the validator every append passes through; tetanus has none, so one fold states each rule once and five differently shaped runs - plain, three turns, a contained tool panic, a provider failure, an interrupt - are checked against it. Until now the exact sequence of one mock turn was asserted (`turn_flow.rs`) and the shape of a journal nothing wrote was asserted (`properties.rs`), but no case said that whatever the turn did, the journal it left closes every turn and step it opened. |
| 2026-08-20 | A DeepSeek stream now runs under an idle window (`DEFAULT_STREAM_IDLE_TIMEOUT_MS`, `DeepSeekConfig::stream_idle_timeout_ms`, TC-DS-IDLE-1..4). Until now the adapter set no timeout of any kind, so a provider that accepted a connection and then said nothing was a turn that never ended: no step boundary was reached, so the interrupt could not cut it either. Five minutes of silence is upstream's own figure, and the failure is `TIMEOUT`, which the default retry policy asks again on. |
| 2026-08-20 | The product identity ported from `llm/llm` (TC-PORT-ATTR-1..5) and sent: `crates/turn/src/llm/attribution.rs` renders `tetanus/<version> (+url)` out of the crate manifest, and the DeepSeek transport carries it on every request. Until now tetanus named itself to no provider. |
| 2026-08-20 | The two `tool JSON parse` cases ported from `coverage-edges.spec.ts` (TC-PORT-ARGS-1..4). No behaviour changed: arguments that are not JSON already arrived as the text the model wrote, and an empty arguments string already read as no arguments. Neither had a case, so either could have been repaired away by a later change without a test noticing. |
| 2026-08-20 | The cap's reason crossing the published boundary is now asserted at the boundary (`crates/engine/tests/max_tokens.rs`, TC-CAP-1 and TC-CAP-2). The turn-level cases pin what the loop does; these ask the same calls a surface asks, so they pin what a caller reads: `agent.prompt` answers `Other("max-tokens")` and the durable `turn/end` carries the same word, the cut-off turn dispatches no `tool/call`, and it still releases the session, because a caller whose answer was cut off has every reason to prompt again. `docs/interface-contract.md` §6 names both clauses. |
| 2026-08-20 | What a settings document overrides ported (`crates/config/tests/upstream_settings_merge.rs`, TC-PORT-SET-1..5). tetanus has no merge step: `file::read` flattens a document into dotted keys and `Config` resolves one key across the layers, so the deep merge upstream performs is a property of the pair and nothing asserted it. The cases pin the four answers a user depends on - one leaf written keeps the rest of its section, a list is replaced whole rather than merged element by element, an empty section sets nothing, and a key written with no value is a null that does not fall back - and the one place the flat model differs from upstream's schema, which section 3 now carries as the open question it is. |
| 2026-08-21 | The idle window's failure is now driven to recovery end to end (TC-PORT-XPORT-6, closing upstream's stalled-body case in `transport-recovery.spec.ts`). TC-DS-IDLE pins the timeout on the adapter alone; this drives the real adapter, decoder, recovery point and retry executor together against a loopback endpoint that sends the head and then goes silent, so a stall becomes `TIMEOUT`, the retry re-sends the same request, and the turn recovers. The `transport-recovery` row's only remaining gap is upstream's whole-request deadline, which the idle window does not cover. |
| 2026-08-21 | A session can be forked (`session.fork`, contract §4.4.6; `crates/engine/tests/fork.rs`, TC-PORT-FORK-1..12 and TC-FORK-1..2), opening `fork.spec.ts` in section 4 and closing it in the same pass. A child journal is the parent's prefix with the parent's `session/start` replaced, one line for one line, by the child's own - which is what keeps `seq` equal to the index of the line and lets the copied `sourceEventSeqs` stand unrewritten, since nothing ever cites seq 0. The boundary must be a closed one, judged from the log alone: the last `turn/start` or `turn/end` at or before it decides. The child is opened through the ordinary create path, so it is listed, paged, titled and prompted like any other session and its first turn is numbered after the turns it inherited - a fork is a resume of a prefix. `tetanus_session::seed` is the one new writer: an append assigns `seq` and `time`, and a copied event has to keep the ones it was written under. A mutation check confirms the suite bites: dropping the open-turn rule fails TC-PORT-FORK-10, and copying the parent's header line as well fails nine of the fourteen. |
| 2026-08-21 | The durability barrier and session-event containment ported (`crates/turn/tests/upstream_scoped.rs`, TC-PORT-SCOPE-1..6), opening `scoped.spec.ts` for `core/session` in section 4 and closing it in the same pass. `TurnEngine::flush` has dispatched `session/flush` since phase ① and nothing asserted any of it: not that every participant is awaited, not that a panicking one leaves the rest to run, not that what a participant appends is in the journal when the caller continues. A mutation check both confirms the suite bites and bounds what it claims: dropping the dispatch fails three of the six cases, while moving the engine's own `log.flush()` before the dispatch fails none - `JsonlSessionLog` fsyncs each record as it appends it, so the trailing sync commits nothing the appends did not, and the claim with teeth is attendance. The sync stays, because `SessionLog` is a seam and a batching implementation would need it. |
| 2026-08-21 | The tool pipeline's scheduling ported as properties (`crates/turn/tests/properties_tools.rs`, TC-PROP-TOOL-1..6), the third `properties.spec.ts` this workspace has an answer for and the last item of the `core/*` gap that was a test gap rather than a missing surface. Until now the scheduler was pinned by five example schedules; a cap, a run of parallel calls and a barrier interact, and no example put a barrier in a pool that was still draining - which is exactly the `started > 0` break in `run_tool_group`, and exactly what the mutation check's shrunk counterexample is. Overlap is reconstructed from an ordered start/end trace rather than sampled with a peak counter, so a claim is decided at every instant the schedule reached. No engine change: it found nothing, which is the expected result for rules five examples already pin, and the value is that they now hold for schedules nobody wrote down. |
| 2026-08-21 | The WebSocket carrier authenticates (`crates/rpc/src/auth.rs`, TC-WSAUTH-1..8), enforcing contract section 4.1.2. Until now `websocket::serve` accepted any TCP connection and handed it the whole `Engine` - turns that run tools and spend money, every journal in the server's directory, the resolved configuration - with no token, no origin check and no per-call authorization. Two attacks, neither exotic: a server bound off-box is reachable by anyone who can route to it, which the standing rule makes the expected deployment; and the same-origin policy does not restrict WebSocket connections, so a page the user merely visits can open `ws://127.0.0.1:<port>` and drive the agent. `Origin` is therefore checked in every posture, because "local" and "trusted" stopped being the same thing the moment a browser could be the local peer. Default deny: the weakest posture offered still refuses every off-box peer and every browser origin, and there is no constructor that admits everyone. Refusal happens before the upgrade, which is why the contract adds no error code - an unauthenticated peer never reaches the JSON-RPC layer. A token travels in `Sec-WebSocket-Protocol` or the URL because a browser's WebSocket API cannot set headers, and the subprotocol wins when both are sent, since a URL reaches more logs. Upstream is unauthenticated and this is a deliberate difference, recorded in section 5. |
| 2026-08-21 | The approval seam implemented (`crates/turn/src/approval.rs`) and ported (TC-PORT-APPR-1..12, TC-PORT-REPAIR-11..13), serving the engine half of contract section 4.4.7. Until now every registered tool ran the moment the model named it, and the only protection a deployment had was not registering the tool. Three rules carry it, and each is a defect if it is quietly relaxed: the seam fails closed, so a grant is one specific word from an answerer that ran and returned and everything else denies; the `never` policy is applied by `request` itself rather than by a listener, because a listener registered later could be ordered ahead of a gate listener and answer first, which is what upstream's three `prepend` cases exist to pin; and the audit pair is one to one, so a `never` refusal still writes both halves and the journal says a question was put rather than saying nothing happened. The policy is a fold over the journal, so a resumed session is under the policy it was under with nothing to replay but the log. `waterfall` stays loud on the bus, so the containment of a panicking answerer is this seam's own: a question that cannot be answered has a defined answer, and unwinding would fail the turn rather than deny the call. Crash repair grows one closer to match, `cancelled` rather than `unavailable` because nobody was found to be missing - the process holding the question died - and it is written before the `tool/result` of the call it was about, which is the order a live turn writes them in. Nothing calls the seam yet: wiring it into the tool pipeline and serving `approval.set` are the next slice. |
| 2026-08-21 | A decision listener that panics ends its turn instead of unwinding into whoever asked for it (`TurnError::Plugin`, TC-PORT-REG-5 and -6, TC-FAULT-10), closing the last containment item in the `core/*` gap. The bus is unchanged and `serial` and `waterfall` are still loud on purpose: a listener that decides something is not instrumentation, and a caller that asked one question should hear that it could not be answered. What changed is the blast radius. Before this the panic escaped `run_turn`, so the journal kept an unbalanced `turn/start` and the session was wedged open - a reader could not tell the turn was over, and the next `session.create` synthesized `interrupted` closers for a turn nothing had interrupted. The turn now fails the way any other failed turn fails, with `step/end` and `turn/end {stop_reason: "failed"}` on the journal, and the engine keeps its turn numbering so the next turn is ordinary. `AssertUnwindSafe` over the step loop is deliberate and narrow: a panic leaves `Progress` describing what the turn had done when it stopped, which is exactly the value the closers need. A mutation check confirms the suite bites: removing the containment fails TC-PORT-REG-5 and -6. |
| 2026-08-21 | Path containment implemented (`crates/turn/src/fs.rs`) and ported (TC-PORT-FSC-1..12), the first of the `fs/*` and `sandbox/*` rows and the piece every later filesystem or shell tool has to stand on. It is containment and not a security boundary, and the module says so: the operations are this process's own and only the path is model-controlled, so canonicalize-then-contain is a complete answer to *this* surface, while isolating untrusted code stays a kernel problem nothing here solves. Resolution walks component by component rather than canonicalizing once, which is what makes two cases come out right - a path that does not exist yet is judged by the directory it would be created in, so a create is fenced before it happens, and a link reached after a missing segment is still followed rather than normalized away. The identity fallback recognises a root reached by another valid name, widening what counts as the root and never the fence. Writing the cases found one defect: a workspace rooted at a regular file was accepted. The mode layer over this fence, and the filesystem service and tools themselves, stay phase ②; the mutating tools are deliberately sequenced after the approval gate rather than before it. |
| 2026-08-21 | Argument validation ported as the published helper upstream actually has (`crates/turn/src/schema.rs`, TC-PORT-SCHEMA-1..10 plus a totality guard). Until now a tool's declared schema meant nothing after the model read it: every tool body either wrote its own defensive parsing or trusted a value the model invented. The slice was nearly shipped as a dispatch gate, and backing that out is the finding worth recording: upstream exports `validateArgs` for tool authors and its agent loop only parses, which is why its own `tool JSON parse` case asserts that a call whose arguments are not JSON still reaches its tool. Gating dispatch failed TC-PORT-ARGS-3, the faithful port of that case - so the gate would have been an invention contradicting a port rather than completing one. TC-PORT-SCHEMA-12 now states the gap deliberately instead of leaving it as an omission, and a hard gate stays a behaviour change that needs a contract clause first. The supported vocabulary is exactly upstream's, and an unrecognised keyword constrains nothing rather than being refused, so a tool using something this validator has not learned is never made to look wrong. |
| 2026-08-21 | The session-projection seam implemented (`crates/session/src/projection.rs`) and ported (TC-PORT-PROJ-1..12), which is the phase ② item two other rows were explicitly blocked on. A unit contributes mathematics and nothing else - `init`, `apply`, `view` - while the registry owns the driving and the watermark, and that split is what lets a checkpoint exist: a value is reproducible from the log alone, so a stored one is only ever a shortcut. The two rules that make it safe are the ones the mutation check attacks: a row whose `state_version` is not this unit's is discarded rather than folded forward, because forward-applying a state that means something else turns one bad checkpoint into a permanently wrong value no new event corrects; and a row claiming events the log in hand does not have is discarded too, because it describes a history this caller cannot show and folding the tail onto it would splice two of them. State is JSON so a row can actually be persisted, and TC-PORT-PROJ-12 serializes one rather than taking the precondition on trust. The usage, pressure and breakdown units are the next slice; nothing registers a projection yet. |
| 2026-08-21 | The tool-result pruner's content transform ported (`crates/turn/src/prune.rs`, TC-PORT-PRUNE-1..9), opening the `compaction/*` row. Tool results are the part of a session that grows without limit - one read of a large file can cost more context than the conversation around it - and nothing bounded them. The transform keeps the head and the tail, which is where the information is, and says in the middle that something was removed; it has no summary because a summary needs a model, and that would make it non-deterministic, unreplayable and billable. Two rules carry it. Measurement and slicing are both in characters, never bytes: the offsets come from a budget rather than from the text, so they land mid-character routinely, and the obvious implementation panics on a UTF-8 boundary - the mutation check confirms it does. And a budget whose head, marker and tail exceed its own threshold is refused where it is set, because a prune that emitted more than it accepts would make the context larger and would never converge for a caller that prunes until it fits. Applying it to a turn's history, and the durable record of a prune, are the next slices; nothing calls it yet. |
| 2026-08-21 | Running one external command implemented (`crates/turn/src/process.rs`) and ported (TC-PORT-PROC-1..10), opening the `subprocess/*` row. It is the primitive under a shell tool, an out-of-process hook, an MCP server on stdio and a subagent driver, and it deliberately registers nothing, so nothing a model says reaches it yet - the model-facing shell tool is sequenced after the approval gate, not before it. The environment design is inverted from upstream on purpose: Node hands a child `process.env`, so upstream inherits and scrubs a denylist, while a denylist has to recognise every secret a deployment might have set and the one it misses is handed to a program a model chose. Here a child gets what the caller listed, and `inherit_env` is the opt-in whose name is the warning. Two more rules: output is bounded and the bound keeps the tail, because the end of a stream is where the error and the summary are; and a timeout is an outcome rather than a failure, so what the command printed before it was killed still comes back. The termination gap is stated rather than implied - this kills the child it started and not its process group, so grandchildren survive, and closing that needs a platform dependency the workspace does not have. |
| 2026-08-21 | A whole-request deadline added beside the idle window (`DEFAULT_REQUEST_DEADLINE_MS`, `DeepSeekConfig::request_deadline_ms`, TC-DS-DEADLINE-1..4), closing the last liveness gap in the `llm/*` row. The idle window catches a provider that stops speaking; nothing caught one that never does. A route answering a byte every four minutes resets the window on every byte, so the turn ran for ever - the same unbounded turn the idle window was added to prevent, reached from the other side. Ten minutes by default: longer than any legitimate completion including extended reasoning, short enough that a wedged route fails while someone is still watching. A zero reads as the default rather than as a deadline of no time, exactly as the idle window reads one, so "unset" and "unbounded" stay different things. Both bounds are `TIMEOUT` so the retry policy treats them alike, and the message names which was reached because that is what tells a reader whether to raise the budget or to ask why the provider went quiet. Upstream has no equivalent, so this is the gap section 3 named rather than a port. |
| 2026-08-21 | The environment layer implemented (`crates/config/src/env.rs`, TC-ENV-1..8). `Layer::Env` has existed since the layered config did, `tetanus config` has always been able to render `env` as a provenance, and nothing ever set a key there - the documented stack of defaults, file, environment and flags was three layers with a hole in the middle, so a container or a CI job had no way to set a value short of writing a document. `TETANUS_AGENT__MAX_STEPS` now does it, with `__` separating segments because a single underscore cannot tell a section boundary from a word boundary in keys like `agent.max_parallel_tool_calls`. A name with no separator sets nothing, which is what stops `TETANUS_HOME` becoming a setting called `home`. Values parse as JSON when they can, so numbers and booleans arrive as themselves rather than failing type resolution as strings, and TC-ENV-4 pins the quoting escape that bargain costs. The tested surface takes its variables as an argument: reading the real environment in a case would mutate state every other case in the binary shares. |
| 2026-08-21 | A refusal the account cannot spend its way out of is told apart from one a backoff fixes (`QUOTA`, `CONTEXT_WINDOW_EXCEEDED`, TC-PORT-QUOTA-1..6). Both arrive as ordinary provider refusals and both were being retried: a dead key spent the entire retry budget before reporting `RATE_LIMIT`, which points a reader at throughput when the problem is billing, and an oversized request was retried although it cannot fit next time either. `LlmError::code` reads the provider's own words before the status, since one status covers both an empty account and a caller going too fast. Neither new code is in the default retryable set, and TC-PORT-QUOTA-5 asserts that as behaviour rather than trusting the name. Upstream classifies with regular expressions; this normalizes - lowercase, punctuation and separators as spaces - and matches phrases, which needs no dependency and accepts the same wordings. The false-positive direction is guarded hardest: `limit` is deliberately not a resource, because reading a rate limit as an exhausted account would fail a turn that a backoff would have fixed. |
| 2026-08-21 | A watcher for the settings document (`crates/config/src/watch.rs`, TC-WATCH-1..9 plus -3b and -7b). `recompose` has been able to re-read a document since it was written and nothing ever called it, so a user who edited `settings.yaml` while the harness ran saw nothing happen until a restart. It polls, which is upstream's mechanism too - a `stabilityThreshold` is a poll - and it needs no dependency and behaves the same everywhere. A change is reported only once the file has stopped moving, because an editor that truncates and rewrites is momentarily an empty document and a watcher firing on the first event would hand a running harness the defaults. A bad edit is reported, refused by `recompose`, and the watcher keeps watching: one that stopped there would make a typo permanent until a restart, and the user's next action - fixing it - would appear to do nothing. The decision is a function of two observations rather than a thread, so a case writes a sequence of file states out literally instead of racing a filesystem's timestamp granularity. The mutation check found two blind spots and both are now cases: a settle count of three was never distinguished from two, and nothing called `Stamp::of` in a way that noticed it read a modification time at all. |
| 2026-08-21 | The preset roster implemented (`crates/config/src/preset.rs`, TC-PRESET-1..10 plus -5b), opening the `preset/*` row. A preset is a directory holding an ordinary settings document, so a deployment that wants a cheap short-budget profile beside an expensive thorough one stops keeping two copies of one document. Roots are searched most-trusted first and the earlier one wins a duplicate, so a shipped preset cannot be silently replaced by a user directory of the same name - and the one that lost stays in the roster marked shadowed, because otherwise "my preset does nothing" has no answer anywhere. A candidate that is not a working preset is reported rather than skipped, since a preset that simply fails to appear gives its author nowhere to look; a directory that was never trying to be one, like `.git`, is skipped instead, and writing that case is what found that a leading dot passed the id rule. A root that cannot be read is a fault and not an empty root, which is the rule the session store and the key-value store already follow: absence and refusal are different facts, and answering "none" would serve a deployment a configuration it did not choose. |
| 2026-08-21 | Workspace instructions implemented (`crates/turn/src/instructions.rs`, TC-PORT-INSTR-1..12), opening the `context/*` row. A repository that keeps an `AGENTS.md` is telling whoever works in it how to work there, and an agent that never reads one re-learns the same conventions every session. Files are read from the project root down to the working directory, so nearer guidance comes last and the preamble says so in words too - ordering alone is a convention a model may or may not honour. The search stops at a `.git` marker, because reading above it pulls a parent checkout's or a home directory's conventions into a project that never asked for them. The security-relevant rule is that content is escaped before it is framed: instruction files come from whoever can open a pull request, the block is delimited, and a file containing that delimiter's closing tag would otherwise end the block early and have everything after it read as harness instruction rather than project guidance. A candidate is a file name and never a path, so a settings document cannot point the search outside the project it is bounded to; the budget drops whole files rather than cutting one, because a truncated instruction can invert its own meaning. |
| 2026-08-21 | A durable key-value store implemented (`crates/core/src/storage.rs`) and ported (TC-PORT-STORE-J1..J13), opening the `storage/*` row. The session log answers what happened; this answers what a run worked out - a projection checkpoint, a computed title, a cache - each reproducible from the log and expensive enough to keep. Four rules carry it: tables are declared at open so a typo is a caller error rather than a table nobody reads; the file is replaced whole by an atomic rename after an fsync, with the directory fsynced behind it; memory is rolled back when a publish fails, so a caller that ignores the error still reads what a fresh open would read; and nothing is written until something is stored. Writing the mutation check found the gap worth recording: three mutations were caught immediately, but replacing the atomic rename with a copy passed every case in the suite - atomicity is the headline promise and nothing pinned it. TC-PORT-STORE-J13 now does, by inode identity and by a reader held open across a publish, which is the observable difference between swapping a file in and rewriting one underneath its readers. Nothing calls the store yet; persisting projection checkpoints is its first intended caller. |
| 2026-08-21 | The filesystem service, its tools, and the permission gates on the tool pipeline (`crates/fs`, TC-PORT-FS-1..50; `crates/turn/src/questions.rs` and the gate in `crates/turn/src/engine.rs`, TC-PORT-INT-1..28), opening the `fs/*` and `interaction/*` rows. Every failure is a class with a code and a sentence a model can act on, because an `io::Error` string makes each caller above the backend parse a message to learn whether the path was missing, the file was binary or the fence refused - and a message is not a contract. The two backends share one containment walk, the one `crates/turn/src/fs.rs` already carries, so fenced and unfenced differ only in which root a path is judged against; `danger-full-access` selects the unfenced backend rather than becoming a branch inside the fence whose job is to skip the fence, which is the one branch a mistake in would be silent. The read-before-write policy is the rule that a session may not overwrite what it has not read, and it is keyed per session because borrowed knowledge is the blind write the rule exists to stop. The gate sits after `tools/pre-execute` and before the dispatch, so what is decided is what would actually run: approving one call and executing another is the failure a gate exists to prevent. Headless was the case that had to work rather than the case to leave for later - with no answerer the ask settles `unavailable`, the call is refused, and both halves of the pair are on the journal, so an unattended run neither hangs nor grants and a reader can see what was asked. Where tetanus parts from upstream is the widest preset: upstream bundles the `never` policy with full access because there `never` means "do not prompt", while here it settles every ask `rejected`, so that bundle would make the widest preset refuse the calls the narrower one allows. |
| 2026-08-21 | Process execution implemented as `crates/exec` and ported (TC-PORT-PROC-1..18, TC-PORT-SHELL-1..19, TC-PORT-TERM-1..14, TC-CLI-SHELL-1..3), taking the `shell/*`, `terminal/*`, `subprocess/*` row from one unregistered primitive to a seam a model can actually call. The termination gap the old row stated is closed the only way it could be: every child leads its own process group, and a kill is a SIGTERM-to-SIGKILL ladder over that group, so grandchildren die with their parent and a command that ignores SIGTERM still ends. Writing the cases found a second hazard nobody had named - a background process inherits the output pipe, so a leader that exits does not close the stream and a reader waiting for end-of-file waits for the orphan; the group is now swept and the caller is told it had to be, which is the difference between a turn that returns and one that hangs for the length of a `sleep 30`. Two backends sit behind one trait so Windows is a backend that is absent rather than a platform the seam cannot express, and a missing binary refuses loudly instead of falling back to another shell, because a bash script run under dash fails later, elsewhere, with a message about syntax. Persistent shells keep the working directory and exported variables between tool calls, bounded by a per-command nonce marker rather than by prompt detection; where upstream resets a dead shell and prints a notice, this keeps the notice and drops the reset, because a silent restart hands the model a shell in a state it did not create while the transcript says everything succeeded. The interrupt needed one composition change to be true rather than nearly true: `boot_with` lets a composition supply the turn's stop switch, the shell tools hold it, and the engine mints one per session - a switch shared across sessions would let an interrupt in one stop another. Moving the seam out of `crates/turn` also exposed a defect in the code it moved: a capture cut mid-character dropped one byte of a partial character and rendered the rest, and the old case could not see it because `printf '\u00e9'` is not POSIX and the shell behind `/bin/sh` printed the escape literally - a multi-byte case that had been running on ASCII. A PTY and the terminal tools that need one, background jobs, and spill files stay out, named in `docs/parity-updates/shell-process-execution.md` with the reason. |
| 2026-08-21 | Sandboxing implemented (`crates/sandbox`) and ported (TC-PORT-SANDBOX-1..19), opening the `sandbox/*` row from "None". The policy is upstream's three modes as one value resolved at a boundary and handed down whole, with the writable-root derivation in one place for upstream's own reason: two layers deriving it separately is how "the write tool cannot write /tmp but bash can" happens. Two things upstream leaves to its backends are said in the policy here - a network decision, because Landlock governs TCP from ABI 4 and a deployment wanting an offline build has nowhere else to say so, and whether partial enforcement is acceptable, because deciding that per backend is how a host silently gets less confinement than was asked for. The Linux backend is Landlock with the syscalls made by hand, and the reason is the fork/exec split: the ruleset is built in the parent where allocating is safe, and the child's half between `fork` and `exec` is three allocation-free system calls, because after a fork in a threaded process a child that allocates can deadlock on a lock another thread held. Deny-by-default is the ABI's own shape - the handled set is every right the kernel knows - which is what makes create, remove and rename governed rather than only write. Every confinement case restricts a real thread or a real child and then performs the operation, so a backend that never made a syscall would fail all twelve; the host here speaks ABI 8. Writing them found two things worth recording: `tempfile::tempdir()` lands under `TMPDIR`, which `workspace-write` grants on purpose, so the first two "denied outside the workspace" cases were writing into a granted root and failing correctly against a backend doing exactly what it promised - the fixture now picks a directory outside every grant and asserts that it is one; and the persistent shell was not confined at all, because the call that would have done it was written and never wired in, with a dead-code warning as the only evidence. Windows and macOS get a refusal that names the upstream backend a port would restate, never a no-op that reports success: upstream's ACL family is ~1500 lines across FFI, token, ACL and runner, and nothing in a workspace without a Windows CI host can prove any of it. The filesystem half is a named follow-up rather than a guess - `Policy` and `landlock::confine_current_thread` are the two pieces it needs, both written - and so is the approved-escalation retry, which belongs with `tetanus_turn::approval` rather than inside the sandbox crate. |
| 2026-08-21 | The sandbox policy wired into the filesystem service (`crates/fs/src/kernel.rs`, TC-PORT-SANDBOX-20..27), closing the follow-up the sandbox slice named and making "one policy, both seams" true rather than intended. The arrangement is not the obvious one and the reason is worth keeping: Landlock restricts a thread irreversibly, and the harness has to go on writing its journal and answering its socket, so the process's own threads cannot be restricted - what is confined is one worker thread that restricts itself before it accepts any work, and every file operation runs there. TC-PORT-SANDBOX-22 pins the consequence that makes it the right shape, by writing harness files while the file service is confined. The central proof is built so it cannot pass by accident: TC-PORT-SANDBOX-20 sets the fence deliberately WIDER than the policy, so `SandboxedFs` resolves the path and permits the mutation - this build's own check says yes - and only the kernel refuses; the same write through the same backend without the layer succeeds, which is what attributes the refusal to Landlock rather than to the fence. The class it lands in was already waiting in the fs lane's vocabulary: `FS_PERMISSION_DENIED` means the operating system refused, as against `FS_SANDBOX_DENIED`, which means this build decided, and a model can act on the difference. TC-PORT-SANDBOX-23 is the requirement behind both lanes - one `Policy` value cloned into the file service and the process executor, refusing the same path on both sides, which is the asymmetry upstream's `roots.ts` note exists to prevent. One worker means file operations are serial; that cost is taken knowingly, because a pool needs one restriction per thread, and widening it is its own slice. The approved-escalation retry remains a follow-up, and belongs with `tetanus_turn::approval` rather than inside either enforcement layer. |
| 2026-08-21 | Swept the `shell/*`/`terminal/*`/`subprocess/*`, `sandbox/*` and `fs/*` rows against the code that now exists, closed the one gap clause that was still buildable, and rewrote the three rows to say what is true. The closure is the per-call escalation stamp (TC-PORT-SANDBOX-28..31), which both earlier notes had named as the remaining follow-up: upstream's escalation is an approval protocol over a policy, and both halves already existed here, so it is `Policy::widened_to` meeting `tetanus_turn::approval` rather than a new mechanism. The `shell` tool declares `Permission::Ask` for a call carrying `sandbox_permissions`, so the question goes through the engine's existing gate and is audited as the same asked/decided pair as every other decision, with the command, both modes and the model's own sentence in the reason; the grant applies to one call and the wider executor is dropped with it, because a cached one is a grant that outlives the question that bought it; a malformed request - unpaired, unknown mode, or not actually wider - is refused in words with no question put at all, since an unanswerable question is worse for the person answering than a refusal; and the two arguments are advertised only where a wider mode exists to ask for. Writing the cases caught one passing for the wrong reason: the refusal case first asked for `workspace-full-access`, which is not a mode, so validation refused it before the answerer ever saw it, and it now asserts the audit pair to prove which of the two said no. Everything else in the three rows is genuinely blocked on something nameable rather than merely undone - a PTY dependency, a job store, a storage policy for spill files, a first consumer for raw piped stdio, a Windows or macOS host to prove a backend on, a container for read confinement - and each clause now says what it waits on, so the slice that lands that thing can close it instead of a future sweep re-deriving why it is open. The `fs/*` row is clear of everything this lane owned: its kernel-sandbox clause is served, and what remains is the fs lane's own list. |
| 2026-08-21 | A real pseudo-terminal (`crates/exec/src/pty.rs`, TC-PORT-PTY-1..8), which the previous sweep had named as the blocker for everything left in the terminal row. Four things it owns because none can be built on the others: allocation, where the child `setsid`s and takes the slave as its controlling terminal - without that second step it has a tty on its descriptors and no session to signal, which is the half-working state worth avoiding; size, set at allocation so nothing starts up believing it has the 0x0 screen an unconfigured pty reports, and resizable afterwards the way a terminal emulator does; signal delivery to the foreground process group, because `^C` on a terminal goes to whichever group owns the terminal and a harness that signalled the session leader would kill the shell every time the model meant to interrupt a command; and a read loop that drains continuously rather than on demand, since the kernel's pty buffer blocks a child that outruns its reader and the only way to lose bytes there is to stop reading. Past our own bound the beginning is dropped and the loss is reported, never silently shortening the transcript. The bounded buffer is factored out of `session.rs` into `transcript.rs`, because a pipe-backed shell and a terminal-backed one have the same hazard and should not have two answers. Three defects found while writing it, all in this slice's own code: `allocate` called `CString::from_raw` on a stack buffer, which would have freed memory nothing allocated; `wait` returned at process exit while the drain was still running, so a caller reading the transcript immediately after would intermittently miss the last thing the program said, and it now waits for the terminal to close with `wait_for_exit` kept for callers that mean only the process; and the signal case first asserted that a shell carries on through an interrupted command list, which `bash` does not do - it abandons the list when a foreground command dies of `SIGINT` - so the case now drives the session the way a terminal tool would, starting a command, signalling its group, then sending another command and watching the shell answer. The tools over the layer - viewport, scrollback paging, an owner-scoped registry - are the next slice and the row still carries them. |
| 2026-08-21 | The built-in feature tools implemented (`crates/features`, TC-PORT-TODO-1..11, TC-PORT-GOAL-1..12, TC-PORT-PLAN-1..7, TC-PORT-FEED-1..6, TC-PORT-SKILL-1..12, TC-PORT-WS-1..5, TC-PORT-ATTACH-1..10), opening the `skill/todo/goal/plan/feedback/attachment/workspace` row. Every one of them keeps its state in exactly one place, the append-only journal, so a fold over the log is the state and a replay reproduces it - a cache beside the journal is a second copy that can disagree with it, and each module ships the case that proves the reload. The todo list is replaced whole every call because a per-item protocol needs stable ids the model has to remember across steps, and it is cleared by the next turn rather than by the end of this one so a finished checklist stays readable where a person looks for it. A goal change names the revision it is changing: a model completing a goal it read three steps ago may be completing an objective nobody set, and compare-and-set turns that into "read it again" instead of a silent wrong answer. Plan mode changes the prompt and never the tool catalogue, because a tool that appears and disappears between steps leaves the model working out which world it is in. Skills follow the precedence rule `crates/config/src/preset.rs` already settled - earlier root wins, the loser recorded as shadowed, a broken candidate reported rather than skipped - and `disable-model-invocation` is checked at the call as well as in the catalogue, so a model that guessed a name does not get a skill a person runs deliberately. Attachments judge the whole batch before storing any of it, since a half-admitted batch leaves the turn seeing part of what was attached with nobody able to say which part is missing, and an image is measured from its header so the pixel limit lands before the decode that would cost the memory. |
| 2026-08-22 | The surface vocabulary over the feature state (`crates/features/src/view.rs`, TC-VIEW-1..10). Not a port: upstream's panels read a generated protocol client, so what there is to restate is the shape a surface reads through, and the tetanus seam that carries the same decision is a serde type folded from the journal. `SessionView` folds todos, goal, plan, feedback and attachments in one pass, because four public folds a caller composes are four chances to draw four panels from four different moments; `WorkspaceView` is separate because it is read from the disk and changes when the disk does, not when the session does. Three rules are pinned by cases rather than by prose. Every view says how far it folded (`as_of_seq`, -1 for an empty log), since a live panel receiving two folds out of order otherwise cannot tell which is newer and a stale one cannot say that it is stale. A view carries no bytes: an attachment is named, measured and content-addressed, and `view::attachment_path` says where the content is - base64 in a fold is a frame nobody can read, a log line nobody can grep and a memory spike on every subscriber, for a thumbnail the surface wants once. A view carries no presentation: markdown arrives exactly as the model wrote it, because the surface knows the width and the theme and a guess here is a guess every consumer has to undo. `null` and empty stay different everywhere they appear, which is what lets a panel tell "no plan yet" from "a plan the model emptied" and "this is a project" from "this is a directory". Two fields that wanted an enum - `todo.status`, `goal.phase` - are strings, so a value added later renders as itself instead of failing to parse the whole view. The cases assert the JSON and not the struct, because the field names are the contract and a rename would leave the struct compiling and every consumer broken. Nothing in `crates/protocol` changes: publishing these over the boundary is two calls, the structs in `protocol::types` and possibly a push, and `docs/contract-updates/features-ui-surfaces.md` section 3 names all three so the presentation lane can say what it needs before that is settled. Writing it removed a second copy of the attachment store's layout - `view::attachment_path` delegates to `attachment::object_path` rather than joining the id itself. |
| 2026-08-22 | The terminal tools over the pseudo-terminal (`crates/exec/src/terminal.rs`, `sanitize.rs`, `terminals.rs`, `terminal_tools.rs`; TC-PORT-TERM-15..40, TC-CLI-TERM-1), closing the clause three earlier notes in this lane had all named as next. The decision worth recording is that readiness is *announced* rather than inferred: upstream watches its terminal for silence and probes whether the foreground process is blocked reading, then decides a command has probably finished, while the shell here is told to print an OSC 133 marker before every prompt - so a send settles when the shell says the command is over, and the marker carries `$?`, which silence never could. Upstream's two guesses stay as the fallbacks they should have been, for a program that prints no marker at all, and both are named in the answer so a caller knows which it got. Three defects and one hazard came out of writing it, all in this slice's own code: an interrupted command still owes a prompt, and the marker arrived after the send had answered - settling the *next* send instantly, with an empty viewport, reporting a command that never ran as finished - so the interrupt path now waits for the prompt it just caused; a background job started in a terminal is in its own process group under job control, so closing the session by killing the leader's group left a `sleep` running, and closing now sweeps the terminal's whole session through `/proc`; a session nobody holds any more could not hang up, because the read task keeps the master open until the child ends and the child is waiting for input that will never come, so `Drop` sends `SIGHUP`; and the viewport carried the caller's own command twice, once from the terminal's echo and once from the shell redrawing the line, which `stty -echo` at startup ends. The owner-scoped registry is enforced now rather than retrofitted - an `Owner` is an opaque name the composition chooses, because tetanus sessions have no agent identity yet, and the engine gives each session its own registry - since the day a sub-agent appears is the day two agents share one and neither can tell. Registering the six tools in `crates/cli/src/main.rs` cost 432 structural points for forty-five lines, exactly what `CONTRIBUTING.md` warns about, so the binary's whole tool composition moved to `crates/cli/src/tools.rs`. What stays out is named in `docs/parity-updates/shell-terminal-tools.md`: background jobs and their store, spill files, the presentation half of a live terminal card, a screen model for programs that draw with cursor movement, and a prompt marker for a PowerShell that `-NoProfile` will not let us configure. |
| 2026-08-22 | A refused answer's provider request id, which the previous sweep of this lane's areas measured and left open (`crates/turn/src/llm/mod.rs`, TC-PORT-REQID-1..5 and TC-REQID-1..6). It is the one fact about a refusal a harness cannot reconstruct from what arrived - the status is on the response, the words are in the body, the classification is a function of both, and the id exists only in the provider's own logs - and it is the only thing a person can quote to a provider's support. tetanus read those same headers for the wait a throttled provider asks for, took the body, and dropped the id with the response. The header rule is a list rather than a name because there is no standard header; `cf-ray` is in it, last, because a refusal generated at a CDN edge never reached the provider and carries none of the others, and a blank header is skipped rather than taken as the answer, which is the same rule upstream enforces by refusing an empty id at construction. The id travels three ways for three different readers: to a recovery listener on `RequestFailure`, onto the `llm/retry` record for the case nobody is watching - a refusal a policy recovered from is reported to no one, so the journal is the only thing left that can say what was refused - and into the published error's `data` for the refusal that ends a turn. `data` and not the message, because section 4.5 lets a surface replace the message with its own wording keyed on the code, so a fact in the sentence is a fact a conforming surface may delete. The wait stays unpublished and the contrast is the argument: a surface can render "retrying" from what it already has, and nothing can render an id it was never given. One case exists because a new field on an error is a chance to change what that error means by accident: TC-REQID-5 holds the classification identical with and without an id, across every status whose code differs. Rendering it is the presentation lane's, in `crates/cli`, and is deliberately not here. |
| 2026-08-21 | Claude Code's hook configuration ported (`crates/hooks/src/claude_code.rs`, TC-HOOK-CC-1..14), the first of the two adapters. The module is one parser with two deliberate failure policies: lenient about shape, because a settings file is hand-edited and one bad stanza must not stop the harness booting, and strict about matchers, because an uncompilable matcher means that group's hooks never fire and a hook a deployment believes is guarding something but which cannot match is worse than no hook at all. Three rules interact and their order is load-bearing - an unsupported event is ignored before its groups are read, a matcher on an event with no matchable subject is discarded before it is judged, and a group with nothing runnable is dropped before its matcher is judged - so a fatal error never fires over configuration that could not have had an effect. Two cases go beyond upstream: a bad matcher on an unrunnable group is not fatal, pinning an ordering upstream's code has but its suite never asks about, and substitution does not re-scan its own output. A mutation check confirms the suite bites: moving the emptiness check, judging subjectless matchers, defaulting the hook type to something other than `command`, accepting a bad matcher, and substituting only the first occurrence each fail one or two cases. |
| 2026-08-21 | The hook output codec ported (`crates/hooks/src/codec.rs`, TC-HOOK-CODEC-1..24). A hook answers in two channels at once, its exit status and whatever it printed, and decoding is deliberately total and lenient: a hook is someone else's program and a turn must not die because one of them printed something unexpected. The exit status frames everything - 0 may carry a structured answer, 2 is a block with stderr as the reason, anything else is an error that does not block, and none at all means it could not be run, which is kept distinct from running and saying nothing so a failed spawn cannot read as an approval. Exit 2 is authoritative and structured stdout is not even read on it, because a hook that both blocked and printed an approval has contradicted itself and the blocking channel is the one that fails closed. The event guard is the security-relevant half: a `hookSpecificOutput` block that names a different event, or names none, has its event-scoped fields discarded, so a stray `PreToolUse` denial cannot deny a `Stop` - while the claimed name is still recorded, because a diagnostic saying a block was discarded is only useful if it can say what the block claimed. Two cases go beyond upstream: decoding is total over twelve hostile payloads across all four exit states, and the event guard cannot be bypassed by writing `deny` at the top level, which is the consequence upstream pins in two halves and never states. One named non-observable: the `starts_with('{')` fast path cannot be mutation-detected here, because a trimmed JSON object always starts with `{` and `serde_json` rejects the rest regardless. |
| 2026-08-21 | Codex's hook configuration ported (`crates/hooks/src/codex.rs`, TC-HOOK-CX-1..13), the second adapter's parse. The shape rules match Claude Code's and for the same reasons - lenient about malformed entries so one bad stanza cannot stop a boot, strict about a matcher on a runnable group so a hook that could never fire is refused rather than silently dead - and four things differ by dialect rather than by policy: five served events rather than seven, with a real Codex event this adapter does not serve dropped like an unknown one because serving half of it would be worse; no parse-time substitution, since a `${VAR}` is the shell's business later and rewriting it would change the command the deployment wrote; `async: true` refused and recorded, because a background hook's answer would arrive after the decision it was meant to inform; and two accepted spellings of the timeout. Three cases go beyond upstream: this dialect judges matchers as regexes, which is the difference that actually changes which hooks fire and which the mutation check confirms by failing two cases when the other dialect judges; a lone async hook empties its group without its matcher then being judged; and `async: false` still runs, since only the literal `true` means background. |
| 2026-08-21 | Detached hook runs ported (`crates/hooks/src/detached.rs`, TC-HOOK-DET-1..8), completing `hook-protocol`. Some hook points are fired and forgotten, and those runs still own a process and still have a continuation that appends to the journal - so disposing an adapter mid-run risks a process outliving the harness, a late append into a closing journal, or a shutdown that blocks for the hook's full ten-minute timeout. The tracker answers all three by cancelling before it waits: a still-running hook is killed rather than waited out, and the wait is for continuations. The drain re-checks in waves rather than waiting on one snapshot, because a run's continuation can track another run while the first wave settles. `CancelSignal` is a small owned primitive rather than a crate dependency, since the seam handed to an executor should not force a crate choice on whoever implements it. Three cases go beyond upstream: a run actually observes the cancellation, without which the whole module could be a signal nobody consults; a signal fired twice keeps the first reason, since the first cause is the diagnosis and a later one its consequence; and waiting on an already-fired signal returns at once. The mutation check earned its place here - TC-HOOK-DET-4 initially passed against a single-wave drain, because on a current-thread runtime the first run completed before the drain took its wave, so the late-tracking path was never reached; the case now orders the sleep so the drain is parked when the late run is tracked. One named test gap: the two-statement window in `cancelled` between checking the flag and awaiting the notification cannot be pinned deterministically, and is recorded rather than faked. |
| 2026-08-21 | The `hook/*` journal events ported (`crates/hooks/src/events.rs`, TC-HOOK-EVENT-1..12): the `hook/invoked`/`hook/result` pair that makes what a deployment's hooks did auditable after the fact. They are log-only and turn-enclosed, correlated by `handlerId`, and written by the protocol rather than by each adapter so the two dialects cannot drift on what a result means. Three payload rules each answer a question a reader would otherwise have to guess: `matcher` is omitted rather than null for a match-all hook, because "matched everything" and "matched this pattern" are different facts; `exitCode` is omitted when the hook could not be run, so a failed spawn cannot read as a clean exit; and `decision` is always present, falling back to `stop` for a halt and `pass` for silence, so nobody has to infer "nothing happened" from an absent field. The stderr summary is trimmed, capped and marked when cut, and the cap counts characters - upstream counts UTF-16 units and counting bytes would panic mid-character, which is the defect the tool-result pruner's port already fixed. Two cases go beyond upstream: the cap is exercised over two-byte, three-byte and surrogate-pair text, and a zero cap is asserted to still report that something was printed. A mutation check confirms the suite bites: byte slicing, dropping the `stop` fallback, always writing the matcher key, and making the cap inclusive each fail one or two cases. |
| 2026-08-21 | The hook stream invariants ported (`crates/hooks/src/invariant.rs`, TC-HOOK-INV-1..11): the pairing, enclosure and field rules that make the `hook/*` audit trail trustworthy. A result must answer an invocation that actually happened, at its own point in its own turn - one handler configured at two points is two pairs, and the correlation key is the triple, not the handler id. Both records must sit inside the turn they name. Upstream enforces this as a cordis plugin that refuses a bad append; this workspace has no invariant registry and `upstream_session_invariants.rs` already records that choice for the session store, so the rules are folded over a journal instead and each `toThrow` becomes a reported fault. The consequence is stated rather than implied: a fold checks producers, it does not guard against them, and growing an append-time validator is the session store's question and not the hooks block's. Upstream's `WeakMap` dispatch staging and session-adoption plumbing exists to make an append-time check work inside cordis, is not a rule about hook events, and is not ported. Two cases go beyond upstream: a duration that is absent or wrong-typed is the same fault as a negative one, since those are what a producer is likelier to write, and every fault is reported rather than the first, because a producer being fixed wants the whole list. A mutation check confirms the suite bites: ignoring the turn mismatch, accepting an unpaired result, keying pairs by handler id alone, accepting a negative duration, and accepting any dialect each fail one to three cases. |
| 2026-08-21 | The hook matcher ported (`crates/hooks/src/matcher.rs`, TC-HOOK-MATCH-1..11), opening the `hooks/*` block and the `crates/hooks` crate. A matcher is the pattern beside each configured hook that decides whether it fires, and the two dialects read one pattern differently: Claude Code treats word-and-pipe patterns as literal alternatives, so `Bash` selects `Bash` and not `BashOutput`, while Codex has no literal path and reads the same pattern as `/Bash/`, which selects both. Getting that split wrong fires a hook for a tool it was never configured for, which is why it is two cases on the same input rather than one. Matching contains its own failures - an uncompilable pattern selects nothing rather than panicking, because matching happens inside a running turn - and `matcher_diagnostic` is the parse-time half that catches the pattern before it gets there. The engine is the `regex` crate rather than JavaScript's, so lookaround and backreferences are refused where upstream accepts them; that is recorded as a deliberate difference, and TC-HOOK-MATCH-11 pins the diagnostic and the matcher against each other so the refusal can never become a hook that is accepted and silently never runs. A mutation check confirms the suite bites: dropping the literal fast path, narrowing the match-all sentinels, panicking on a bad pattern, and blinding the diagnostic each fail two or three cases. |
| 2026-08-21 | The hook decision merge ported (`crates/hooks/src/merge.rs`, `src/types.rs`, TC-HOOK-MERGE-1..14). Several hooks can match one point and disagree, and the fold is deliberately not a vote: the most restrictive answer wins, because a hook exists to stop something and one that is outvoted has not been heard. Four rules, each with a separate reason and each defended by the mutation check: permission is `deny > ask > allow`, expressed as an ordering on the enum so the fold is `max` rather than two switches that must agree - swapping two variants in the declaration fails two cases; only the winning answer's reasons surface, because explaining a refusal with the text of an unrelated `allow` would misdescribe it; a halt is sticky and keeps the *first* halting hook's reason, later halts being implied by it; and context and warnings accumulate in hook order with empties skipped, unjoined, because what separates them belongs to whoever renders them. Two cases go beyond upstream's suite: the decision is order-independent across every pair of the five decision spellings, which matters because hooks run concurrently in a later slice and arrival order stops matching configuration order, and inserting a silent observe-only hook anywhere leaves the outcome identical, which is what a deployment relies on when it adds logging to a point that already has a gate. |
| 2026-08-21 | The hook stdin payloads ported (`crates/hooks/src/payload.rs`, TC-HOOK-PAY-1..12): what each dialect tells a hook is happening. Upstream has no payload suite - these shapes are asserted incidentally by `bridge.spec.ts` driving a whole agent - so porting them as their own module is what puts the two dialects side by side, which is where the parity risk lives. Four differences would each silently break a real hook, and each is now a case: an absent transcript is `""` in Claude Code and `null` in Codex; `tool_input` is the arguments verbatim in one and narrowed to `{command}` in the other; every Codex payload carries `model` and `permission_mode`; and the turn-scoped Codex events carry `turn_id` as a string. Three cases go beyond upstream, all about what a hook reads when something is missing rather than wrong. The bridges themselves - registering these against `PreToolUse` and friends - are deliberately not ported: the turn engine has no interception points to register against, building them is turn-engine work that overlaps the tool pipeline, and inventing a private set here would be a second competing interception surface. The split is clean and nothing in this module changes when the wiring lands. |
| 2026-08-21 | The hook runner ported (`crates/hooks/src/runner.rs`, TC-HOOK-RUN-1..12). It is thin plumbing over an executor, and the rule worth the slice is what it does when the executor fails: a hook that could not be run at all becomes an outcome with no exit code and the fault on stderr, which the codec's rules make non-blocking because only exit 2 blocks. A deployment whose hook binary is missing must not find every tool call denied, and TC-HOOK-RUN-11 pins that over three failure shapes - the mutation check confirms it, since decoding a fault as exit 2 fails two cases. The executor is a narrow `HookExecutor` trait this crate declares rather than a dependency on the shell crate: running commands is a service this crate consumes, a separate lane owns it, and upstream's own suite duck-types the same seam for the same reason. Two more cases go beyond upstream: a failed run is still timed, because the duration is durable on `hook/result` and a gap there is exactly where a reader will look; and an absurd per-hook timeout saturates rather than wrapping, since the seconds-to-milliseconds conversion multiplies a configured value and a wrapped one would silently give a hook microseconds. |
| 2026-08-21 | Delegation depth ported (`crates/subagent/src/depth.rs`, TC-SUB-DEPTH-1..11), opening `crates/subagent` and the `subagent/*` rows. An agent can start a child that can start its own, and without a budget that recursion has no floor - a persona that always delegates would spawn agents until the machine stopped, each spending real money on real model calls. The rule carrying the safety is that depth is monotone: it is the larger of the persisted header value and the runtime one, so runtime may deepen the count and can never shorten it. That is not a tie-break. A resumed child is constructed with fresh options, and believing the runtime value outright would let a child resumed with nothing set count itself as top-level and delegate on a full budget; the mutation check confirms it, since preferring the runtime value fails that case. Upstream validates at every use because a JS number can be `-0`, `NaN` or `1.5`; here the type is `u64` and those are unrepresentable, so the same rule is enforced once at the JSON boundary where such values can still arrive. Three cases go beyond upstream: an unset cap is unlimited rather than zero, since zero forbids delegation and reading unset as zero would silently disable every subagent; a whole number written as a float is accepted because JSON does not distinguish `2` from `2.0`; and the count saturates rather than wrapping, a wrap being the same top-level-budget failure reached by arithmetic. |
| 2026-08-21 | The subagent descriptor ported (`crates/subagent/src/descriptor.rs`, TC-SUB-DESC-1..12): the durable `subagent/descriptor` record that says which provider established a child and whether it is a one-shot run or a resumable conversation. Upstream has no descriptor suite - its rules are exercised through the service - so porting the record alone is what makes them assertable without one, the same payload-versus-wiring split the hook bridges used. The rule pair worth naming is that an unknown version reads as absent while an undeclared field at the current version is refused: a newer runtime's child is not classifiable here and that is not the same as broken, whereas an extra field at this version is corruption whose quiet acceptance would ignore composition somebody asked for. That is also why the record snapshots a closed list of named fields rather than the options object - an unrelated extension's value must not be able to make a resume fail. Three cases go beyond upstream: the first record wins, since scanning to the last would let a later append rewrite a child's identity; a written record reads back identically, which is what a cold resume depends on; and an absent field is omitted rather than written null, because a null would be refused on the way back in. The service, continuation and list-children suites are assessed and deliberately not ported: each needs a provider or agent registry surface this engine does not expose, and standing up a private one here would be a second competing surface. |
| 2026-08-21 | Delegation lifecycle invariants ported (`crates/subagent/src/invariant.rs`, TC-SUB-INV-1..11): the provider-registry and run-pairing rules the parent's bookkeeping rests on, since a run that starts twice or ends without starting corrupts it silently rather than loudly. The subtle rule is the last: `subagent/end` repeats a run's whole identity rather than only its id, and if the repeat disagrees then one of the two events is about a different run, which means a parent may be crediting an answer to the wrong child. As with the hooks block and the session store before it, the rules fold over a recorded stream instead of refusing a dispatch, because this workspace has no invariant registry. That fold exposes something upstream's code hides: it validates the end identity even when no start was found, which is unreachable there because the missing-start failure throws first, so the identity check is skipped here to keep every orphaned end from carrying a second meaningless fault. Three cases go beyond upstream: a run id is reusable once its run closed, since the registry tracks open runs; a run may outlive the removal of the provider that started it, which upstream states in a comment and which a stricter check would wrongly report; and an orphaned ending reports one fault rather than two. |
| 2026-08-21 | A child's final answer ported (`crates/subagent/src/assistant_output.rs`, TC-SUB-OUT-1..10). When a delegated run ends the parent needs one thing back, and a journal holds several candidates; picking wrong is how a parent reports an empty result for a run that plainly said something. The rule is the last non-empty assistant message, else the accumulated streamed text, else nothing. Two details carry it: *last*, because a child that kept working after an intermediate answer meant the later one, and *non-empty*, because the loop appends an empty message to record usage after a step with no visible output and letting that win would erase a real answer. Selection ignores the stop reason - an interrupted child still said what it said. Adapted to this journal's vocabulary, where a message's content is a string and a chunk is tagged, which additionally means the reasoning channel is visible to the fold and must be excluded. Three cases go beyond upstream, all about a live or damaged read: the fold answers the same way when asked twice, text arriving outside the journal joins the same fallback, and a malformed record is ignored rather than failing the parent's run. One equivalent mutant is named rather than papered over: the empty-piece guard in `push_text` cannot be distinguished by any case, because `collect` already treats an empty fallback as no answer. |
| 2026-08-21 | Settling a finished child run ported (`crates/subagent/src/settlement.rs`, TC-SUB-SETTLE-1..9). A one-shot delegated run is a background task from the parent's side, and something has to say what happened and free what the child held. Two rules earn the module: disposal always happens, because a settlement that returned early on a bad result would leak a child process for exactly the population most likely to be numerous; and a failure to release does not hide the failure that caused it, so when both fail both survive with the cause first - reporting only the disposal error would replace the diagnosis with an after-effect. An unknown stop reason is a failure named by itself rather than a refusal to classify, because a run that stopped for an unrecognised reason has still stopped and leaving it unclassified would leave the parent waiting. The `Killed` outcome carries no output field at all, so an aborted run cannot report a partial draft as its answer; upstream relies on the mapping never populating it, here it is unrepresentable. Four cases go beyond upstream: an aborted and a failed run each report their reason and not the child's text, a completed run with no output still completes, and a stop reason round-trips through the text form it crosses a process boundary in. |
| 2026-08-21 | The child timing projection ported (`crates/subagent/src/timing.rs`, TC-SUB-TIME-1..10), and it is the first unit in this workspace to implement the `Projection` seam outside the session crate's own tests - the drain's projection slice noted that nothing registered one yet. A parent showing a child's elapsed time needs a number that survives a resume, and the journal is the only thing that does. Two rules carry it. The `subagent/descriptor` record is the child's origin, so reaching it resets settled time to zero: a child's journal can open with inherited history whose turns are the parent's work, and without the reset a forked child reports its parent's elapsed time as its own from the moment it starts. The reset keeps a turn that is already open, timed from its original start, because that turn is what created the child. And journal time is not monotonic - timestamps are wall-clock, written across processes and possibly across a clock adjustment - so a turn that appears to end before it began contributes zero rather than a negative, since a running total that shrank would make a reader distrust every number on the page. Five cases go beyond upstream, including that the state round-trips and folding on from the reloaded value equals never having stopped, which is what makes this a projection rather than a function. |
| 2026-08-22 | The enumeration half of listing a parent's children ported (`crates/subagent/src/children.rs`, TC-SUB-KIDS-1..14), and with it the engine slice that serves contract section 4.4.9 - `spawned_by` and `depth` on `session/start`, which the contract had explicitly left for a later engine slice to write. The field shape is the whole lesson of the slice. This started as one `origin` field with a kind beside it (`fork` or `subagent`), which reads as sufficient and is not: the contract had already ruled it out, for the case that breaks it. A fork of a subagent's journal is *both* - it inherits the `spawned_by` of the work it copies while recording its own `parent_session` - so a single origin field either classifies it as a fork and loses it from its spawner's listing, or classifies it as a subagent and loses the copy lineage. Two fields answer the two questions lineage exists for: what else came out of this conversation, and why does this session exist. Delegation is therefore exactly the `spawned_by` edge and parentage is never reported as it. Traversal is a separate question and crosses both edges, so an agent started from a fork of this conversation is still discovered while the fork itself is not reported - a walk following the delegation edge alone would stop at the fork and silently lose every running agent below it. Ordering is oldest first with the id breaking the tie, because enumeration order is whatever the directory returned and a listing read twice must not reshuffle itself; creation time needs no header field, being the `session/start` event's own timestamp. Every walk is iterative and bounded by a visited set that starts holding the root, because the corpus is files in a directory anyone can edit: a cycle, a link to a session that is not there, and a chain deeper than a call stack are all reachable without anything in this process going wrong, and a parent that hung - or aborted - while listing what it had delegated is the worst available answer. A fork inherits `spawned_by` and `depth` unchanged and writes its own `parent_session` and `fork_seq`, which is section 4.4.9's rule and not a new one. `depth` is `u32` in the engine header to match the width `crates/protocol` parses the same line at. Three cases go beyond upstream: the self-parent cycle an off-by-one in a spawner produces, liveness carried through rather than inferred since it is not a fact of a journal, and the persisted `depth` told apart from the walk's distance - a distinction upstream has no counterpart for because it has no separate spawn edge. Deliberately not ported: upstream's row materialization, which resolves each child's mode and label from its descriptor through the projection registry and diagnoses a settled child whose descriptor cannot be read - that needs the projection registry joined to persistence inspection, `crates/subagent/src/descriptor.rs` already carries the record it reads, and nothing here changes when it lands. `cwd`, the third origin fact, is still a gap: it needs a session to have a working directory. |
| 2026-08-22 | The agent inbox ported (`crates/turn/src/inbox.rs`, TC-INBOX-1..16): the two queues that hold input arriving while the loop is busy, and the durable splices they are folded from. A person types while the agent is working; that message cannot join the request in flight and must not be dropped, so it waits for a boundary that can carry it. Two queues rather than one because the boundaries differ - `next-turn` holds prompts each wanting a turn of their own, `next-step` holds input joining the turn already running, and merging them would either run steering as a fresh turn or bury a prompt inside one. A turn boundary claims all of `next-step` and exactly one queued prompt: a person who queued three questions asked three questions. Two rules carry the durability. The journal commits before the projection moves, so a journal that refuses the append leaves the queues exactly as they were - serving a message from memory that no restart remembers is worse than never accepting it, because the person watched it join the queue and has no reason to type it again. And what is recorded is the *normalized* splice, never what the caller said: a record holding `-1` would be re-derived at replay against a list of a different length and reconstruct a different queue from the same journal. A claim is a pure deletion while a cancel records `canceled`, so a reader can tell what a person withdrew from what the loop delivered. Identity is unique across both queues together, because two entries answering to one name make cancelling one of them a coin toss. Clearing cancels `next-step` before `next-turn`, so a crash between the two writes leaves the prompts - which still mean what they said - rather than steering aimed at a turn that may already have ended. Adaptations: upstream publishes notifications through a Cordis service and these are returned from the mutation, this workspace having no notification service and a private one being a second surface competing with the journal; and upstream's id lives on its `UserMessage` while `tetanus_turn::llm::Message` is the provider wire shape, so the id is on the pending entry instead. Upstream normalizes `NaN` and fractional coordinates because a JS number can be either; `i64` cannot, so the type enforces that rule and the surviving coordinate cases are ported. Six mutations, all caught, including the ordering one - moving the append after the projection fails the refused-journal case. The consumers are next: `tools/post-execute` has no additional-contexts channel yet, which is what the hook bridges and the repeat-tool guard both wait on. |
| 2026-08-22 | Closed the rest of the `shell/*`, `terminal/*`, `subprocess/*` Gap column: spill, the piped seam, session-owned terminals, and the collectable half of background work (TC-PORT-PROC-19..26, TC-PORT-SHELL-20, TC-PORT-TERM-41, TC-CLI-SHELL-4). Three of the four were waiting on something that has since arrived, which is the argument for sweeping a row rather than re-deriving it: spill was blocked on a storage policy and the storage lane wrote one, so this put the producer on the other end - and the producer is the only one who can, because a bounded capture drops its beginning while the command runs and a policy above the seam would file the tail and call it the output; the artifact is opened on the first overflow and never before, so a command whose output fits touches no filesystem, and because the buffer still holds everything at that instant what lands on disk is the complete stream. Piped stdio was waiting on a first consumer, and `crates/mcp` arrived by spawning its own child - which killed the server and left a server's own helpers running - so the seam closed a leak rather than tidying a duplicate, and MCP's suite is green over it unchanged. Owner-scoping was waiting on "a session having an owner", which turned out to mean the composition had no way to be told which session it was building for: `SessionTools` now carries the session's id and the directory of its journal, so a terminal is owned by the session that opened it and a spilled build log lands where a reader is already looking. Background jobs are answered in two halves rather than deferred whole: on a terminal the session is already the collection point, so `wait_ms` makes "start it and come back" a thing a model asks for (silence deliberately does not settle a short wait, and zero is refused because a send that waited no time would report an empty viewport as no output), while `run_in_background` for the one-shot `shell` really does need the `jobs/*` store - a one-shot's group is swept when its call returns - and the note names the three things that store must offer rather than building a second one here. What remains is a screen model for programs that draw with cursor movement, a PowerShell prompt marker that `-NoProfile` will not let us configure, and a Windows host; `docs/parity-updates/shell-row-closed.md` says what each would take. |
| 2026-08-22 | The executor a deployment's hooks run through (`crates/exec/src/hooks.rs`, TC-PORT-HOOK-1..6), closing the second half of the process row's piped-stdio clause - the half that named out-of-process hooks. `crates/hooks` had declared `HookExecutor` and deliberately implemented none, so the only one in the workspace was a recorder in its own suite: a deployment could configure a hook and nothing would ever run it. A hook now gets what every other child here gets, and the case that matters starts a watcher inside a hanging hook and asserts the watcher dies with it, because a hook runs inside a turn and one that leaks is a turn's worth of orphans per event. Two defaults differ from upstream on purpose: the environment is a list rather than a scrub, since nothing is inherited here and a denylist exposes every credential added after it was written - the case puts a secret in this process and watches it not arrive - and the timeout ceiling is the hook protocol's ten minutes rather than the shell tool's, because clamping a configured hook to a model command's budget would shorten it silently. The cases drive `run_hook` rather than the executor alone: what has to be true is not that a command ran but that the protocol reads the result as a hook, so a `2` blocks with its reason and a hook that never ran blocks nothing and says why. The bridge - registering hooks against `PreToolUse` and the rest - stays with the `hooks/*` row, which waits on interception points in the turn engine; what changes for that row is that its bridge slice now constructs an executor instead of writing one. |
| 2026-08-22 | A credential typed at a terminal is no longer written down (`Tool::recorded`, TC-TOOL-RECORD-1..4, TC-PORT-TERM-42). Raised by the presentation lane, who analysed it properly and ruled out the obvious wrong fix: `submit: false` is not a password signal, and screen-only redaction would hide what the journal keeps rather than remove it. The shape built is theirs - a `secret` flag honoured at record time - with three changes from the note. The seam is a `Tool` method rather than a case for one tool's name, because the engine must not know what an argument means and because `shell`/`shell_run` leak the same way through a command line. Their second option, the engine detecting a password from the `[wait: stdin_read]` sequence, is not built and the reason is measured rather than asserted: the reliable signal is the tty's `ECHO` flag, `tcgetattr` on the pty master reads identically before, during and after a real `read -s` prompt, and this crate's own `stty -echo` at session start would have erased the transition anyway - a detector that fails silently in the safe-looking direction is what the note rejects on the presentation side. And their third option ships *with* the first rather than instead of it, because a flag the model forgets is a leak and the floor has to be stated where the model reads it. Writing the case found a second leak nobody had named, which is why it asserts on the whole journal rather than on `tool/call`: the model said the password, so `assistant/message` and the streamed `assistant/chunk` carried it too, and redacting the call while keeping the message it came from would have been a fix that fixed nothing. Failing closed here means silence - a redactor that panics withholds every argument, the opposite direction from `ToolMode`'s fail-closed and consistent with it, since a broken classifier costs concurrency and a broken redactor costs a credential. Only the named argument goes: the tool, the session and the moment stay, because an audit that cannot see a secret was typed is not an audit. `docs/contract-updates/shell-recorded-arguments.md` carries the one thing the boundary has to settle - §4.3's "the sentinel is not proof" warning is weaker than what is now true for an engine-minted argument, and the honest fix is the flag §4.3 already defers. |
| 2026-08-22 | The backstop under the credential flag (`looks_like_a_password_prompt`, TC-TOOL-RECORD-5, TC-PORT-TERM-43), decided by the captain from a scout report and built after the flag itself. The argument for it is that a caller flag fails open exactly where it matters - published measurement shows agents follow optional-flag policy rules inconsistently across repeated trials - so the flag is unioned with a runtime rule rather than shipped as a control. The mechanism is `sudo`'s, and the choice is evidence rather than taste: `sudo` had the terminal's `ECHO` flag available, says so in the first line of its own documentation for the feature, and built a regex over the program's output instead; this crate measured the same conclusion twice, since readline holds echo off at its own prompt and `backend.rs`'s `stty -echo` pins it off for the whole session. One narrowing from `sudo`: the match is on the last non-empty line rather than anywhere in the output, because a prompt is by definition the last thing written before a program waits, and `sudo`'s own default arms on a `grep` hit for the word - a false positive it accepts and has no published rate for. The rule lives in the engine and the evidence stays with the terminal: at record time the engine cannot know which argument names a session, and guessing would put tool semantics back where `Tool::recorded` exists to keep them out. Union, never override, per contract §4.3 - which is also the section that specifies the sentinel, correcting the §4.6 citation the original note carried. |
| 2026-08-21 | A second session-persistence backend (`crates/session/src/sqlite.rs`, TC-PORT-STORE-Q1..Q21), opening the persistence half of the `session/*` row. The `SessionLog` trait had been the session seam since phase ① and nothing had ever crossed it: `LiveSession` held a concrete `JsonlSessionLog`, so the abstraction was untested by construction. The engine now holds `Arc<dyn SessionLog>` and `sessions.backend` picks, which is what makes most of the suite an equality between the two backends' answers to one script rather than a second set of assertions. Durability is per append on both - `synchronous = FULL` under WAL fsyncs each commit and each append is its own commit - because a backend a caller cannot tell apart must not quietly promise less than the one it replaced. The database says what it is: an `application_id` and a `user_version` are stamped at creation and checked at open, so an unrelated file is refused rather than grown a `sessions` table, and a future schema is refused distinctly from a stranger's file for the reason the JSON store already gives - one is corrupt, the other may still be running. Migration is lossless both ways to the byte, which TC-PORT-STORE-Q12 pins by comparing the files rather than the events, because both writers serialize the same `SessionEvent`. The backend is resolved at boot rather than at the first `session.create`: a store this build cannot read is a deployment fault, and one that waits for the first turn to report itself is one a user finds first. |
| 2026-08-21 | Context compaction implemented (`crates/turn/src/compaction.rs`, TC-PORT-COMPACT-1..15), closing the `compaction/*` gap. A session that outgrew its window had nowhere to go: the request grew until a provider refused it with `CONTEXT_WINDOW_EXCEEDED`, which is terminal by design, so the turn simply failed. The surface is derived and never stored - nothing is deleted or rewritten to make a span disappear. A `compaction/summary` names the events it shadows, the next surface event replaces them, and `compaction::surface` applies that rule wherever history is derived, so a replay reproduces the compacted history from the same records rather than from a second stored copy that could disagree. The adjacency of a record and its replacement is load-bearing rather than tidy: it is what lets a bounded fold price a replacement with one running total and one pending claim instead of keeping a price per message and growing a checkpoint without bound, and TC-PORT-COMPACT-3 pins that anything between them expires the claim. A cut never splits a tool call from its result, decided over the current surface rather than over step markers, because compaction moves surface positions and step markers do not follow. The cheap remedy runs first: pruning tool results needs no provider and is often enough, so a summary is asked for only when pruning was not. A summary that is not smaller is refused, because committing one leaves the session over budget with a replacement that gets compacted again on the next step for ever. One defect found in the writing: the underflow guard in `select_range` used `then_some`, whose argument is eager, so a session with nothing to compact panicked instead of declining. |
| 2026-08-21 | The five session projections implemented (`crates/turn/src/projections.rs`, `crates/session/src/units.rs`, TC-PORT-PROJ-13..26), closing the two section 4 rows that were marked blocked. The projection seam had existed since TC-PORT-PROJ-1..12 with nothing registered on it. The two that price nothing - title and stats - live in `crates/session`, so a listing that wants a title need not link a provider adapter; the three that price live beside the token meter. Both halves of the split were needed by the same blocker: the request envelope. `request/context` is now written by each step before it dispatches, carrying the route, its context window and what the system prompt and tool catalog cost, and it joins `MOCK_TURN_FLOW` deliberately - after `system-prompt/assemble` because it prices that assembly, before `agent/request` because a listener rewriting the request must not be able to change what the journal already said the request was, and before the answer because a turn a provider failure ended should still say what it tried to send. Two defects found. The usage fold read `turn` and `step` off `assistant/message`, which contract section 4.3.1 does not give it, so every step of a turn looked like a repeat of the first and a whole turn was counted once; a report is now identified by the `step/start` enclosing it, which needs no contract change and is the more robust rule. And TC-CONTRACT-1 asserted that *no* event a turn writes is unparsed, which section 4.3.2 makes impossible to keep while the vocabulary grows - it is now stated over the documented types, with TC-CONTRACT-1b holding the other half, that an unknown type reaches a surface whole. |
| 2026-08-21 | A credential store and a spill policy (`crates/config/src/credentials.rs`, `crates/core/src/spill.rs`, TC-PORT-CRED-1..15 and TC-PORT-SPILL-1..9), closing two thirds of the `storage/*`, `spill/*`, `credentials/*` row. A credential in the settings document is a credential in every artifact - the document is read into layers, published by `config.dump`, quoted in diagnostics and pasted into bug reports - and `crates/config/src/secret.rs` exists precisely to redact values that should never have been there. The store is where they belong: the process environment over an owner-only file, values that never enter a layer at all. The environment wins and is visibly read-only, because a key supplied at launch is this run's explicit intent and nothing in the process can edit it; a write against a reference the environment supplies is refused rather than accepted into a file resolution would then ignore, since a write that appears to succeed while the old value keeps being used is the worst of the three available behaviours. `Secret` is neither `Clone` nor derived-`Debug` and renders as the redaction in both formatting traits, so the one leak that needs no mistake in the store - a struct holding a secret being debug-printed into a log line - produces nothing. TC-PORT-CRED-13 is the acceptance case and it greps a real run's dump, journals and served events rather than asserting on fields, because a typed assertion only checks the fields someone thought of; it also asserts the secret is resolvable, so a store that held nothing cannot pass by vacuity. The spill policy reserves its notice's worst-case cost inside the cap before cutting the preview, for the defect TC-PORT-SPILL-3 sweeps for: spending the whole budget on a preview and then appending a notice makes a replacement larger than the original for a marginally over-cap input, which is the one outcome a size policy must never have. |
| 2026-08-21 | An MCP client implemented (`crates/mcp`) and ported (TC-PORT-MCP-1..20), opening the `mcp/*` row. Tools a server advertises are the first capability in this workspace that neither the harness nor the model owns: a program someone else wrote, started by this process, answering on a pipe. Three rules carry it. A line that is not a message ends the connection rather than being skipped, because newline-delimited JSON has no resynchronisation point and a client that skipped would be guessing where the next message starts - and writing a log line to stdout is the single most common way an MCP server is broken, so the fault quotes it. A call that runs out of budget fails alone: the server is told to forget the request, the connection stays up, and one slow tool does not cost every other tool its server. And nothing this crate starts outlives it - `kill_on_drop` under a close-input-wait-kill ladder, with the departure reported rather than assumed, so TC-PORT-MCP-15, -16 and -18 assert against `/proc` that the child is gone, including on the path where the handshake failed and no client was ever returned. The fixture server the cases talk to is a real program behind a `fixture` feature, so a published build does not ship a test double; the channel-pair cases and the process cases are kept apart on purpose, because a process spent to assert a string is a process spent for nothing. |
| 2026-08-21 | The MCP tool bridge and reconnect supervisor (`crates/mcp/src/tools.rs`, `src/supervisor.rs`, TC-PORT-MCP-21..33). A server's tools now reach the model: they are registered under `mcp__<server>__<raw>` beside tetanus's own and dispatched by the ordinary pipeline, so nothing in the turn engine knows an MCP tool from a native one. Every MCP call is exclusive, deliberately - `ToolMode` is opt-in because a tool may overlap only for arguments it has looked at, and nothing here has looked at anything, so a barrier is the answer that cannot make things worse. The supervisor's budget is the part with teeth: delays double to a ceiling, a cap ends the retrying for good, and the budget resets only on real uptime past that ceiling, so a server that connects and dies four times a second exhausts its cap instead of restarting for ever. Shutdown cancels a pending backoff rather than joining it. Two findings came out of writing the cases. Upstream's public name collides for distinct identities when a server's own name carries the `__` separator, and since a tetanus server name comes from a settings document that is reachable here too, so a separator in a server name now forces the hash. And a reconnect that has launched a process has not yet finished a handshake, which is why the health state and not the launch count is what a caller waits on. |
| 2026-08-21 | Named agent presets applied to a run (`crates/engine/src/preset.rs`, `AgentPreset` in `crates/config`, TC-PORT-PRESET-1..10), closing the half of the `preset/*` row the roster left open. A preset was a directory nobody could use; it now names a model, a provider, a step budget, a tool subset, a prompt shape and a persona, and `session.create` takes one by name. Three rules carry it. The tool subset is applied to the registry that session's turns run on, so the model is never offered a tool it may not call - being offered one and refused is a step spent on a refusal. The id is resolved once, at creation, and written into the session header: a session whose agent changed under it half way through a conversation would leave a journal that is a record of two agents with nothing marking the boundary, and a fork inherits rather than re-resolves for the same reason. And a preset that names a tool the harness does not have is refused where it is used rather than silently narrowed, because a typo that produces a smaller agent is a capability nobody took away. An inline definition in the settings document beats a directory of the same id, which is the roots' own trust order applied one level up. `SessionCreateParams` gains an optional `preset`; the contract note is `docs/contract-updates/mcp-preset.md`. |
| 2026-08-21 | The web tools implemented (`crates/web`, TC-PORT-WEB-1..26), opening the `web/*` row. Both tools sit above one transport seam, and that is the design decision rather than a testing convenience: every rule worth having - the scheme, the redirect, the size, the type, the charset - is a decision made above the socket, so the whole policy is asserted with no network in the suite and the live transport is thin enough to read in one sitting. Three rules have teeth. A URL carrying credentials is never sent, and neither is a redirect to one, because `https://user:token@host/` in a model's output is a credential about to be handed to whoever answers. A hop that leaves the origin is refused, because a page that redirects to an internal address is how a fetch tool becomes a request forgery - TC-PORT-WEB-7 asserts the blocked destination was never requested, not merely that the call failed. And truncation is always stated in the text the model reads, because a model given half a page and no notice answers confidently about the half it did not get. Search resolves rather than picks: two usable providers and no configured choice is a refusal naming both, since answering the same query from a different engine per run is not something anyone can debug from a journal. One provider is real and mapped over the same seam, with upstream's strict mode kept - an answer with no result block is refused, because an uncited paragraph presented as search results is how a citation becomes a hallucination. |
| 2026-08-22 | Folded every lane's note under `docs/parity-updates/` and `docs/contract-updates/` into `docs/parity.md` and `docs/interface-contract.md`, and deleted the notes. Thirty-three notes had accumulated because a lane that edits a shared file collides with every other lane, and a note nobody has folded is a parity claim no reader of the parity document can see: `fs/*`, `interaction/*`, `mcp/*`, `web/*`, `preset/*`, `host/*` and the feature-tool row all still read `None` here while the code that serves them was on `master`. Section 3 is now one row per area - 28 rows where there were 44, because union merges had appended a variant row per slice instead of editing the one that was there, and each spelled the same gap slightly differently. Folding them by hand rather than by appending is the point: the merge that produced the 44 lost three clauses inside rows nobody read to the end, so every dropped duplicate was diffed clause by clause against the row that replaced it and each difference was either carried over or is a clause the code now serves. Section 4 gains 42 port rows the notes had written and nobody had pasted - fs, sandbox and its kernel half, the permission gate, user questions, permission presets, the shell and terminal families, the pseudo-terminal, compaction, the SQLite backend, credentials, spill, the projections, the feature tools, the surface vocabulary, MCP, presets, the web tools and the four `host/*` packages - and the subprocess row's `Ports to` is corrected, since the seam moved to `crates/exec` and the row still named `crates/turn`. Three rows record what is built and not yet composed: the fs, feature and web tools and the declared MCP servers are crates a composition registers, and `crates/cli/src/tools.rs` names none of them, which is a clause in the gap column rather than a claim in the Today column. The contract document gains the fourth carrier, the permission gate's position in the pipeline and the code a denial carries, five durable types with the adjacency rule that makes a bounded fold able to price a compaction, `session.create`'s optional preset, `sessions.backend`, and a new §5.1 naming the two view types that are deliberately not on the boundary yet. |
| 2026-08-22 | Folded the four notes that landed while the reconciliation slice was being written: the request id (`llm/*`), the closing of the `shell/*`, `terminal/*`, `subprocess/*` gap column, the two `hook/*` journal types and the contract half of the request id. The shell row is the one worth reading twice - three of its four remaining clauses are served (spill artifacts that keep the whole stream rather than reporting a loss, a piped seam for a child this harness talks to which `crates/mcp` now starts its servers through and which closed a helper leak, and terminals owned by the session that opened them) and the fourth is half served, because `run_in_background` on a terminal needs no store while the one-shot `shell` genuinely does. What is left is named with what it waits on rather than as a to-do: the job store from `workflow/*`, a screen model for programs that draw with a cursor, a PowerShell prompt marker that `-NoProfile` will not load, and a Windows host. |
| 2026-08-22 | The tool registration surface (`crates/toolset`, TC-TOOLSET-1..10 plus 1b and 1c) and the binary wired to it (TC-CLI-TOOLSET-1..6). Five tool crates had landed and the shipped binary offered none of them: it composed `echo` and the shell tools by hand, so `tetanus tools` listed six entries on a build whose suite was green, because every tool crate tests its own tools and none of them tested the program people run. The tools page now lists twenty. The general lesson is the one TC-CLI-TOOLSET-1 is shaped around: a crate existing, being tested, and being reachable from the binary are three different facts, and only a case that execs the binary can tell them apart. Two findings came out of the wiring. `Source::registered` drains a throwaway registry, so no tool crate needed an accessor added and five cross-lane pull requests did not have to happen. And the first cut composed the registry behind an `expect` on the claim that `tools.sources` was checked when the document was read - nothing checked it, so a misspelled source name panicked; it is now refused against the document that set it, and the check runs once at boot for the path whose closure has nowhere to return a failure to. |
| 2026-08-22 | Folded the four notes that landed while the fold was queued: the tool registration surface, the sandbox settings key, and both halves of what the journal keeps of a tool call. The first invalidated three clauses this document had just gained - `crates/toolset` composes the fs, feature, web and MCP tools into the shipped binary, so "built and not composed" stopped being true the day after it was written, and the rows say so rather than keeping a gap that had closed. The last is a divergence rather than a port and is recorded in section 5 as one: upstream journals its terminal and hook payloads whole, and tetanus lets a tool declare at record time what the journal may keep, with a backstop for the model that does not declare it and a floor - a terminal journal holds whatever was typed into it - that stands whatever the mechanisms catch. |
| 2026-08-22 | The external contract surfaces implemented and ported (`crates/query`, TC-PORT-QUERY-1..19; `crates/sdk`, TC-PORT-SDK-1..12 and TC-PORT-API-1..14; `crates/acp`, TC-PORT-ACP-1..24), closing the `acp/*`, `sdk/*`, `api/*` row's three named gaps and the query half of `session-query/*`. Reading a journal as data derives position rather than carrying it: contract section 4.3.1 puts `turn` and `step` on the structural events and on nothing else, so one forward pass works the boundaries out and hangs them on every event - adding the fields to the journal would be a wire change for something the order of the lines already implies. An absent clause and an empty one stay different questions, because a `Vec` that could not tell them apart turns a filter built from an empty user selection into "match everything", the most expensive possible wrong answer. The SDK is two layers for upstream's reason: a protocol client that enforces the handshake exactly as a carrier does, and an owned-run API over it that exists for one ordering bug - the subscription has to be open before the prompt or the first events are already gone, which a caller writes once and debugs for a week. The gateway is the request surface as data, because the codec's `match` over method names cannot be enumerated and so nothing could answer what calls exist and what arguments they take; writing it found that `method::ALL` omits `agent.steer`, which is declared, routed, capability-named and reserved, and TC-PORT-API-3 pins the omission so the owning lane's fix is noticed. ACP brings no second carrier - it is a `FrameHandler` on the stdio carrier `crates/rpc` already serves, since a second framing implementation is a second place for framing to be wrong - and the ACP session id is the engine's own, so an operator holding one can find its journal and no mapping table can fall out of step. Three deliberate departures: tool activity goes on the ACP wire, because §7.2 already fixes that the journal is the stream and withholding it would build the quieter second history that rule exists to reject; `max-steps` maps to `max_turn_requests` rather than `max_tokens`, since a client told the latter would shorten its prompt and be wrong about why; and a harness rejection is `end_turn` rather than `refusal`, which would put words in the model's mouth. The bridge's client half is a second process on purpose - the suite re-enters the test binary as an agent over real pipes - and it earned that immediately: `close` shut the write handle down while still owning it, so the child never reached end of file and every teardown quietly waited out the ten-second kill fallback before killing a process that would have left on its own. It passed either way; only the clock could tell, and TC-PORT-ACP-22 is now the clock. What stays open is named: image and audio prompts wait on the durable attachment store, ACP session load, fork and resume are unbuilt, and the permission channel is built and pinned on both sides but has no engine-side approval seam to be driven from, which is a contract change rather than a lane change. |
| 2026-08-23 | The served catalogue made one answer (`crates/cli/src/tools.rs::served`, TC-CLI-CAT-12). On a twenty-six-tool build `tetanus serve --frontend` answered `catalog.tools` with one tool while `tetanus serve` answered with twenty-six, so the browser panel and everything on `/api/` saw an empty toolbox. The assembly was one thing; the binary was not, because it builds an engine in five places and only one of them was given the assembly. The lesson is narrower than "share code": a comment saying two things cannot disagree is worth nothing when agreeing is a property of one call site rather than of a function both callers go through. The case is shaped to match - it asks every surface of one build and compares the answers to each other, not to a number, so it fails on disagreement rather than going stale when a tool crate lands. Found by the UI lane, which refused to paper over it with a page-side fallback; that was right, because a fallback there would have made the page correct and left `catalog.tools` lying to every other client. |
| 2026-08-22 | The `tools/post-execute` additional-contexts channel served (`crates/turn/src/events.rs`, `crates/turn/src/engine.rs`, TC-POSTCTX-1..6), closing the clause three separate parity rows pointed at. The event now answers a `PostToolDecision` - an outcome plus contexts - instead of a bare `ToolOutcome`. Two fields because a listener has two different things to say: the outcome is what the model reads as this call's result, the contexts are what it should read next. A guard that made its point by appending to the tool's output would be corrupting a tool author's contract, and a tool parsing its own result back would find a sentence nobody wrote - so the note travels beside the result and arrives as its own `user/message`. Contexts ride the decision rather than being appended by the listener, which is what lets the loop hold them to the commit: a call whose result is never committed, one behind an earlier call's fault, contributes no context either, and a listener writing straight to the journal could not be held to that. They commit after the result and are delivered at the next step boundary through the durable queue, so the complaint never precedes the thing complained about, and a note produced by the step that ended the turn stays queued for the next turn rather than being dropped - the boundary a guard cares about most is exactly where a lossy channel would lose it. A turn whose listeners attach nothing writes no queue record at all, so a deployment that does not use the seam pays nothing and its journals diff clean. The engine now owns its session's queue, folded at construction from the journal's own suffix: `own_suffix` skips a fork seed, because those splices are the parent's and were normalized against the parent's list, so folding them would hand the child a queue neither session ever had. A journal that will not fold fails the engine rather than being absorbed, since running with an empty queue would silently drop input a person is still waiting on; `crates/engine/src/convert.rs` splits the three causes the way the rest of that table does - a refused append reads as the journal failing, a splice that does not apply reads as the journal damaged and carries its seq, and a duplicate identity is this build's bug. Upstream folds contexts onto both an `accept` and a `block` decision; tetanus has one outcome, so there is no second variant and the blocked-call case has no counterpart. Four mutations, all caught. |
| 2026-08-22 | The `core/*` and `hooks/*` gap clauses swept against the code so each names precisely what is missing and why, rather than naming a feature. A clause that is only a noun - "scoped stores", "fiber disposal" - is a blank row wearing a label: it cannot be picked up, argued with, or closed, and it hides whether anything still blocks it. Two findings came out of the sweep and both change what the rows say. **The hooks bridges are no longer blocked on missing interception points.** That clause had been true when written and is now false: `PreToolUse` maps onto `tools/pre-execute`, whose output *is* the call that runs, so `updated_input` has somewhere to land; `PostToolUse` onto `tools/post-execute`, whose decision now carries contexts; `Stop` onto `agent/turn-stopping`, which already takes a veto; `UserPromptSubmit` onto the first `agent/pre-step`. What is actually left is a deny path and an owner for configuration discovery plus the executor binding. **A `PreToolUse` deny has nowhere to go, and it is the same dependency as the tool-permissions clause.** `tools/pre-execute` answers a `ToolCall` and cannot refuse one, and the gate after it fires only when `ToolRegistry::permission` already returns `Ask` - so a hook forbidding a call the registry marks `Allow` would be consulted and then ignored, which is worse than not consulting it, because a deployment would believe the hook was enforcing something. Both rows now name that single dependency: every gated call routed through the approval seam rather than only the ones a registry pre-declares. The remaining clauses each carry their reason - scoped stores and scoped prompt layers wait on a plugin context to scope to, which is the phase ③ host; cancellation inside a step is a contract question about what a half-read stream writes to the journal before it is a code one; a sticky cut-off step waits on the `next-turn` queue; `cwd` waits on a session having a working directory rather than on anything about the journal; runtime-context providers need a provider list the assembly walks, which is a change to `system-prompt/assemble` and therefore its own contract slice. |
| 2026-08-22 | Tool permissions routed through the pipeline (`crates/turn/src/tools.rs`, `crates/turn/src/events.rs`, TC-PORT-INT-10..12), which is the dependency the `core/*` tool-permissions clause and the `hooks/*` `PreToolUse` deny both named. Before this the approval gate was consulted only when `ToolRegistry::permission` already returned `Ask`. That answer is static and written by the tool author, who cannot know a deployment's policy - so a gate reachable only for calls somebody had already flagged could never be *added* to an existing tool, which is exactly what a permission table and an out-of-process hook need to do. Every call now passes a `tools/permission` waterfall carrying the declared answer, and a listener may return a stricter one. Stricter only, and the fold enforces it rather than a convention: `Permission::most_restrictive` ranks `Allow` below `Ask` below `Deny`, because a listener able to widen could quietly un-gate the irreversible call a tool author deliberately gated, and the author is the one who knows it is irreversible. Ties keep the earlier answer's words, so a chain of policies reads in the order it ran. `Deny` is a new variant beside `Allow` and `Ask`, and the distinction from an `Ask` answered no is the point: a policy that already knows the answer must not stage a decision nobody is going to take, since there is no human in a hook's loop and the question would hang or fail closed on a decision already made - so a deny writes no `approval/asked` pair and the model reads the refuser's own words. The seam sits after `tools/pre-execute` and not before, which is the rule the gate already followed: a listener may rewrite the call, and deciding first would permit one command and execute another. `MOCK_TURN_FLOW` gains one line, on purpose and in this commit - the first draft put it before `tools/pre-execute` and the conformance suite caught that the code's order was the deliberate one. Three mutations, all caught. |
| 2026-08-22 | The two tool hook points bridged onto the pipeline (`crates/hooks/src/bridge.rs`, TC-HOOK-BRIDGE-1..10). Every other module in `crates/hooks` is pure - which hooks match, what is written to them, what they may write back, how answers combine - and none of it knows a turn is happening; this is the part that binds those decisions to `crates/turn`'s extension points. The design question worth recording is that `PreToolUse` has two jobs that land in different places: a hook may rewrite the call, which belongs at `tools/pre-execute` because that event's answer *is* the call that then runs, and it may forbid the call, which belongs at `tools/permission`, the only seam that can refuse one. Running the hooks at both points would be wrong twice over - a hook is a program with side effects, so a deployment's audit trail would double, and the two runs could disagree, leaving the call rewritten by one answer and judged by another. So they run once, at the rewrite point, and the merged answer is held by call id for the gate that immediately follows; the hold is taken exactly once, so a retried call reusing its id cannot be judged by a run that was about a different call, and a parallel group is safe because its calls have distinct ids. Absence is inert in both directions, which is the property that keeps the bridge from failing open: a bridge that did not run leaves the declared permission exactly as it was rather than reading as approval, and a hook that *permits* also leaves it alone, because "I permit this" is not "and nobody else may object" - letting it lower the declared answer would un-gate the irreversible call a tool author deliberately gated. `merge_hook_outputs` deliberately has no opinion about a rewrite, there being no such thing as a more restrictive one, so the bridge states the rule instead: the last hook to supply one wins, since hooks run in configuration order and a later entry overriding an earlier one is what every other layered configuration here does. `PostToolUse` contributes one message per hook rather than one joined blob, because the notes came from different programs and joining them would invent a single voice. One fix travelled with the slice: `run_hook`'s clock parameter was `&dyn Fn() -> u64`, neither `Send` nor `Sync`, so the runner could not be awaited inside a bus listener - the one caller it exists for - and the bound is now widened. Five mutations, all caught. |
| 2026-08-22 | The three observation hook points bridged (`crates/hooks/src/bridge.rs`, TC-HOOK-WATCH-1..10), completing all five. None of `SessionStart`, `UserPromptSubmit` or `Stop` carries a permission answer, so none can refuse a tool call - but two can still change what happens, and which two is what decides where each registers. `UserPromptSubmit` may add context to a prompt or refuse it outright, and `agent/pre-step`'s decision is exactly those two options, so it registers there; it fires only on the first step's claim, because a later claim is the loop's own bookkeeping and running a prompt hook over it would tell the hook a person said something they never typed. `Stop` may ask that the turn *not* end, which is the veto `agent/turn-stopping` already takes. `Stop` is also the one inverted point in either dialect: everywhere else a hook's `continue: false` asks the turn to halt, but here the turn is already ending, so the same field is the only way a hook can ask for more work - reading it the usual way round would turn "do not stop yet" into "stop", the exact opposite of what a deployment configured. `SessionStart` can only observe, and that is recorded as a gap rather than approximated: its `additionalContext` is decoded and returned but has no sink, because the only one is the turn's queue and a session has not started a turn when the point fires; a hook whose context was silently dropped would be worse than one that was never run. The port found and fixed a real defect: the payload context is built once when a bridge is composed, so Codex's turn-scoped `turn_id` served that construction-time value, and every hook would have seen every event as belonging to one turn - a hook correlating a `PreToolUse` with the `Stop` that followed would have paired them across unrelated turns. Four mutations; three were caught immediately and the fourth exposed a weak assertion of this port's own, a wordless refusal checked only for presence, so an empty string would have reached a person as a blank rejection reading like a harness bug rather than a configured policy; the case now pins the words. |
| 2026-08-23 | Fixed the reason a turn could not be stopped in any backgrounded deployment (`crates/exec/src/signals.rs`, TC-PORT-TERM-44). It had been visible for days as three tests that failed in the full workspace run and passed alone, which everyone including this lane first read as load. Load was a correlation: a busy machine is also a machine driven by a script rather than by hand. The cause is that a `SIG_IGN` disposition is inherited across `fork` *and* `exec`, and POSIX has a shell set `SIGINT` to `SIG_IGN` for anything it runs in the background - `tetanus serve &`, a systemd unit, a CI runner, an orchestrator - so the bash on the pseudo-terminal inherited the ignore and so did every command it ran. `killpg` kept returning success because delivery genuinely succeeded; the process simply ignored it, and `terminal_signal` answered "delivered SIGINT to foreground process group N" while `sleep 30` slept on. The diagnosis is two runs on one idle machine: from an interactive shell the sleep dies, with `&` it survives, reported delivered both times - which is why the fix is a disposition reset rather than a longer timeout. Every child of all four seams now starts with the ignorable signals at `SIG_DFL` and an empty mask, applied between `fork` and `exec` in syscalls only, as bash, sudo and tmux do at the same point; `SIGPIPE` is in the set because a Rust parent ignores it process-wide, which is right for this process and wrong for a shell. The case reproduces the cause rather than the correlation - it sets `SIGINT` to `SIG_IGN` in the test process and asserts the command still dies - and it bites: removing the fix fails it in 10.9s, restoring it passes in 0.3s. Two further defects fell out. An interrupt must reach the shell as well as the foreground group, because a shell running a list forks one job per iteration, so signalling the foreground kills one `sleep` and the shell starts the next - that case had been passing on an idle machine by accident, since idle timing catches the shell between jobs, which is the one case where the shell does get the signal and does abandon the list. And a send that did not settle on a prompt leaves the shell owing one, which could settle the next send instantly with an empty viewport, reporting a command that never ran as finished; the debt is counted now, replacing a bounded wait a busy machine won and removing a hazard the module had documented as unavoidable. Nothing was ignored, serialised or weakened: the two cases that asserted through the race now wait for the state they are about - the shell reclaiming the terminal, the output stopping - rather than for a fixed 400ms. The rule this leaves behind generalises past signals and is in `AGENTS.md`: what the harness inherits, its children inherit, and the cheap test for the whole class is to run the same thing twice, once interactively and once with `&`. |
| 2026-08-24 | Fixed the PowerShell backend's interactive invocation (`crates/exec/src/backend.rs`, TC-PORT-SHELL-21). The trait's `interactive()` defaults to the pipe invocation, which is honest for a backend with no separate interactive mode and wrong for this one: pwsh's session argv carries `-NonInteractive -Command -`, `-Command -` means "read the script from redirected standard input", and pwsh checks that standard input really is redirected - on a pseudo-terminal it is not, so the process prints its usage and exits 64 before a prompt exists. TC-PORT-TERM-32 asks for a `pwsh` terminal and accepts either answer a host can honestly give, a session or "this host has no pwsh", and got a third: `DiedStarting { backend: "pwsh", status: Exited { code: Some(64) } }`. It was invisible on every developer box here because no host in the fleet has a PowerShell, and red on every pull request because GitHub's `ubuntu-latest` runner ships one. Reproduced locally by putting a PowerShell 7.4.6 tarball on `PATH`, which failed the case identically. The fix is the backend's own: `PowerShell::interactive()` is `-NoLogo -NoProfile`, the REPL, which is what a terminal session wants and what pwsh runs when it is given neither `-Command` nor `-NonInteractive`. The test's expectation was left alone - a present-but-unusable backend is a defect in the invocation, not a third outcome a caller should learn to read. The new case asserts the argv rather than opening a session, because a case that can only fail on the one host that has a PowerShell is a case this fleet would not have run; it fails on every host with the fix reverted. |
| 2026-08-23 | A screen for the programs that draw (`crates/exec/src/screen.rs`, TC-PORT-SCREEN-1..7, TC-PORT-TERM-45), closing the row's own words: the sanitizer strips escapes and keeps no screen, so `htop` and `vim` were runnable here and not readable. Two models of one stream are kept because neither derives from the other - a printing program's transcript is what it said, and a drawing program's transcript is every frame concatenated with nothing marking which forty lines are still true. `terminal_read` picks by the one reliable signal a terminal carries: a program on the alternate screen has announced that it draws. Colour and attributes are deliberately not modelled, since a model reads text and an attribute model would double the file to record something nothing renders. Pointing the cases at the real `vim` and the real `htop` rather than at `printf` fixtures found two defects invisible to every sequence an author would have typed: terminal sessions advertised `TERM=dumb`, which tells a program there is no screen - so the very family this layer exists for was refusing to draw - and a session had no `HOME` or `PATH`, which is right for a one-shot command and wrong for an interactive program, and which made `vim` paint its status line and stop. The second read as a hole in the screen model for an hour; `TerminalConfig::passed` fixes it in the shape this lane already uses for hooks, a list of names that pass rather than a denylist of names that do not. |
| 2026-08-23 | The code runtime rescued and landed (`crates/coderuntime`, TC-PORT-CODERT-1..34, TC-CODE-SET-1..4), after the branch was finished, measured, and then left behind when master moved. Reported as sixteen commits, it was nine: the other seven were the mcp/web lane's, which the branch sat on top of and which landed separately under different SHAs, so the replay was `--onto` the nine rather than a re-litigation of another lane's work. Every line of Rust survived two days of drift unedited and all 47 cases pass, which is the part worth recording: the seams it depends on - the tool trait, the registry, the turn pipeline - did not move under it. What did move is composition. `crates/toolset` did not exist when this was written, and master's rule is that a tool crate is not offered by the binary until it is a line in that crate's `sources()`; so `run_code` is built, configured and tested, and the shipped binary does not offer it. That is not a five-line addition and the note says why: every existing source is independent - `Source::registered` drains a throwaway registry holding only its own crate's tools - while `run_code` exists precisely to call the *other* tools, so `settings::tool` needs an `Arc<ToolRegistry>` that does not exist until `Assembly::build` has already run. Wiring it needs a late source, built from the assembled registry rather than beside it, which changes where the duplicate check runs and what `tools.sources` can switch off, and belongs to the lane that owns `crates/toolset`. One claim was corrected rather than carried: the README row said the runtime is "turned on from the settings document", which today would have a deployment set a key and get no tool; it now says registered by whoever composes a registry, with the binary wiring named in the `Planned` column. `settings.rs` is kept exactly as it is, because it is the function that late source will call. `docs/parity-updates/coderuntime-rebase-drift.md` is the note. |
| 2026-08-22 | The runtime context a turn tells the model (`crates/turn/src/context.rs`, TC-PORT-RTCTX-1 and TC-RTCTX-2..10), which contract section 4.4.8 has specified and nothing has produced. That is the third published-with-no-producer gap this lane has found, after `assistant/message`'s missing turn and step and section 4.4.9's origin facts, and it had the same shape: a page of contract, a row in the section 4.3.2 payload table, a green boundary case (TC-PROTO-25), and no journal that ever carried one. Providers are named, ordered by the deployment and gathered once per turn between `turn/start` and the first `step/start` - once per turn and not once per step, because a snapshot is a fact about when the turn began, so a step that runs for ten minutes works from the time it started with and a tool that changed directory does not retroactively change what the model was told. It is a user message and never a prompt section: a provider caches a prompt by its longest stable prefix, and a sentence saying what time it is would invalidate that prefix on every request of every session. Only the newest snapshot derives to a message, because a hundred turns write a hundred and yesterday's date is worse than no date; the earlier ones stay on the journal, which records what happened. A deployment that configures no providers pays nothing - not a journal line and not a message - which is why the record is skipped rather than written empty. Two departures from upstream are deliberate: the reading is UTC with the zone stated, because a display zone means a time-zone database and this workspace has none, and the providers share one record rather than appending one message each, because "only the newest travels" needs a single record to be decidable. One reading of the clause was settled by a case rather than by argument: the snapshot enters the history where the journal puts it, before the message that opened the turn, rather than last - both satisfy the caching property, and the journal's order also keeps a request ending in the user's ask or a tool's result, which matters because the thing that answers the last message is not only a model but this crate's own mock, which looped when the block went last. |
| 2026-08-22 | A second storage medium behind the same seam and the registry that mounts both (`crates/core/src/storage/`, TC-PORT-STORE-C1..C8, TC-PORT-STORE-S1..S2, TC-STORE-REG-1..3), which the earlier sweep of this lane had parked behind a `Cargo.toml` collision that has since landed. The database is opened lazily, because the file backend promises that a store nobody writes to leaves no trace and a backend a caller cannot tell apart must not quietly promise less; it stamps `application_id` and `user_version` at creation, so an unrelated SQLite file is refused rather than grown a `records` table, and the identity is checked before the version, since a coincidental `user_version` on somebody else's database would otherwise be reported as a version problem and send the reader after the wrong answer. `synchronous = FULL` under WAL is the deliberately slower pragma, for the same reason: the file backend fsyncs a rename per write. The suite is the interesting half. Upstream runs one conformance suite against every backend and the reason is what a seam is for, so each of the eight shared cases here runs its whole body against both media and says which one failed - and it immediately found a defect no single-backend suite could: `json::Store::remove` published the file even when the key was not there, so a run whose only storage call was a defensive remove left a store behind, against that module's own opening rule that nothing is written until something is stored. The registry keeps upstream's two rules that matter: a name mounted twice is refused rather than replaced, because silently keeping the second gives half a deployment's consumers the other medium with nothing in a log to say why, and a stale unmount handle must not remove a successor registered under the same name, which is the failure that only appears under a reload. What is left in the row is the domain layer over both media, which is a layer above this seam rather than a change to it. |
| 2026-08-22 | The durable half of the projection seam (`crates/session/src/projection_cache.rs`, TC-PORT-PCACHE-1..6), and a sweep that found most of what the `compaction/*` and `session/*` Gap columns still claim is already served. `Projections::checkpoint` and `restore` had existed since the seam landed with nothing writing one down, so every reader folded from seq zero on every open - the cost the checkpoint exists to avoid. That is the fourth mechanism-with-no-driver found in these areas and the easiest to miss, because it makes nothing wrong, only slow: a case comparing values passes with the cache deleted, which is why TC-PORT-PCACHE-1 asserts the number of events folded instead. Every rule here is about not trusting the row - a shortcut and never an authority, stale but never wrong, a version mismatch discarding rather than migrating, and every path fail-soft, because a session that would not open because its cache was corrupt would be a session lost to an optimisation. It was buildable now and not before because it needs a key-value store whose medium is the deployment's choice, which landed in the commit before it. The sweep half: `compaction/*`'s three Gap clauses are all served (history fitting, the durable prune transaction, the model-driven summariser), `session/*`'s SQLite persistence and all five projections are served, and what remains in those rows is telemetry - an OpenTelemetry dependency decision rather than a port - export and query, which the row already marks ③, a manual `/compact` that is a surface decision, and a per-model window table that is data models outdate faster than releases. |
