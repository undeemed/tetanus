# The build's output and its inputs. `dist/` is regenerated by `pnpm run
# build`, which `crates/host/tests/panel_port.rs` runs as part of the merge
# gate - so committing it would mean a reviewer reading a minified diff of
# something the gate rebuilds anyway.
dist/
node_modules/
coverage/
